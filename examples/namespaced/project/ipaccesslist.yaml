apiVersion: project.mongodbatlas.m.crossplane.io/v1alpha1
kind: IPAccessList
metadata:
  name: example
  namespace: crossplane-system
spec:
  forProvider:
    cidrBlock: 0.0.0.0/32
    comment: Example IP access list
    projectId: <project ID>
  providerConfigRef:
    name: default
    kind: ClusterProviderConfig
