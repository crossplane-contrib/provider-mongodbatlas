---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: backupschedules.cloud.mongodbatlas.crossplane.io
spec:
  group: cloud.mongodbatlas.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - mongodbatlas
    kind: BackupSchedule
    listKind: BackupScheduleList
    plural: backupschedules
    singular: backupschedule
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BackupSchedule is the Schema for the BackupSchedules API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BackupScheduleSpec defines the desired state of BackupSchedule
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  autoExportEnabled:
                    description: 'Flag that indicates whether MongoDB Cloud automatically
                      exports Cloud Backup Snapshots to the Export Bucket. Value can
                      be one of the following:'
                    type: boolean
                  clusterName:
                    description: The name of the Atlas cluster that contains the snapshot
                      backup policy you want to retrieve.
                    type: string
                  copySettings:
                    description: List that contains a document for each copy setting
                      item in the desired backup policy. See below
                    items:
                      properties:
                        cloudProvider:
                          description: Human-readable label that identifies the cloud
                            provider that stores the snapshot copy. i.e. "AWS" "AZURE"
                            "GCP"
                          type: string
                        frequencies:
                          description: List that describes which types of snapshots
                            to copy. i.e. "HOURLY" "DAILY" "WEEKLY" "MONTHLY" "ON_DEMAND"
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        regionName:
                          description: Target region to copy snapshots belonging to
                            replicationSpecId to. Please supply the 'Atlas Region'
                            which can be found under https://www.mongodb.com/docs/atlas/reference/cloud-providers/
                            'regions' link
                          type: string
                        shouldCopyOplogs:
                          description: Flag that indicates whether to copy the oplogs
                            to the target region. You can use the oplogs to perform
                            point-in-time restores.
                          type: boolean
                        zoneId:
                          description: Unique 24-hexadecimal digit string that identifies
                            the zone in a cluster. For global clusters, there can
                            be multiple zones to choose from. For sharded clusters
                            and replica set clusters, there is only one zone in the
                            cluster. To find appropriate value for zone_id, do a GET
                            request to Return One Cluster from One Project and consult
                            the replicationSpecs array Return One Cluster From One
                            Project. Alternately, use mongodbatlas_advanced_cluster
                            data source or resource and reference replication_specs.#.zone_id.
                          type: string
                      type: object
                    type: array
                  export:
                    description: Policy for automatically exporting Cloud Backup Snapshots.
                      See below
                    items:
                      properties:
                        exportBucketId:
                          description: Unique identifier of the mongodbatlas_cloud_backup_snapshot_export_bucket
                            export_bucket_id value.
                          type: string
                        frequencyType:
                          description: 'Frequency associated with the export snapshot
                            item: weekly, monthly, yearly, daily (requires reaching
                            out to Customer Support)'
                          type: string
                      type: object
                    type: array
                  policyItemDaily:
                    description: Daily policy item. See below
                    items:
                      properties:
                        frequencyInterval:
                          description: Desired frequency of the new backup policy
                            item specified by frequency_type (daily in this case).
                            The only supported value for daily policies is 1 day.
                          type: number
                        retentionUnit:
                          description: 'Scope of the backup policy item: days, weeks,
                            months, or years.'
                          type: string
                        retentionValue:
                          description: 'Value to associate with retention_unit.  Note
                            that for less frequent policy items, Atlas requires that
                            you specify a retention period greater than or equal to
                            the retention period specified for more frequent policy
                            items. For example: If the hourly policy item specifies
                            a retention of two days, the daily retention policy must
                            specify two days or greater.'
                          type: number
                      type: object
                    type: array
                  policyItemHourly:
                    description: Hourly policy item. See below
                    items:
                      properties:
                        frequencyInterval:
                          description: Desired frequency of the new backup policy
                            item specified by frequency_type (hourly in this case).
                            The supported values for hourly policies are 1, 2, 4,
                            6, 8 or 12 hours. Note that 12 hours is the only accepted
                            value for NVMe clusters.
                          type: number
                        retentionUnit:
                          description: 'Scope of the backup policy item: days, weeks,
                            months, or years.'
                          type: string
                        retentionValue:
                          description: Value to associate with retention_unit.
                          type: number
                      type: object
                    type: array
                  policyItemMonthly:
                    description: Monthly policy item. See below
                    items:
                      properties:
                        frequencyInterval:
                          description: Desired frequency of the new backup policy
                            item specified by frequency_type (monthly in this case).
                            The supported values for weekly policies are
                          type: number
                        retentionUnit:
                          description: 'Scope of the backup policy item: days, weeks,
                            months, or years.'
                          type: string
                        retentionValue:
                          description: 'Value to associate with retention_unit. Monthly
                            policy must have retention days of at least 31 days or
                            5 weeks or 1 month. Note that for less frequent policy
                            items, Atlas requires that you specify a retention period
                            greater than or equal to the retention period specified
                            for more frequent policy items. For example: If the weekly
                            policy item specifies a retention of two weeks, the montly
                            retention policy must specify two weeks or greater.'
                          type: number
                      type: object
                    type: array
                  policyItemWeekly:
                    description: Weekly policy item. See below
                    items:
                      properties:
                        frequencyInterval:
                          description: Desired frequency of the new backup policy
                            item specified by frequency_type (weekly in this case).
                            The supported values for weekly policies are 1 through
                            7, where 1 represents Monday and 7 represents Sunday.
                          type: number
                        retentionUnit:
                          description: 'Scope of the backup policy item: days, weeks,
                            months, or years.'
                          type: string
                        retentionValue:
                          description: 'Value to associate with retention_unit. Weekly
                            policy must have retention of at least 7 days or 1 week.
                            Note that for less frequent policy items, Atlas requires
                            that you specify a retention period greater than or equal
                            to the retention period specified for more frequent policy
                            items. For example: If the daily policy item specifies
                            a retention of two weeks, the weekly retention policy
                            must specify two weeks or greater.'
                          type: number
                      type: object
                    type: array
                  policyItemYearly:
                    description: Yearly policy item. See below
                    items:
                      properties:
                        frequencyInterval:
                          description: Desired frequency of the new backup policy
                            item specified by frequency_type (yearly in this case).
                            The supported values for yearly policies are
                          type: number
                        retentionUnit:
                          description: 'Scope of the backup policy item: days, weeks,
                            months, or years.'
                          type: string
                        retentionValue:
                          description: Value to associate with retention_unit. Yearly
                            policy must have retention of at least 1 year.
                          type: number
                      type: object
                    type: array
                  projectId:
                    description: The unique identifier of the project for the Atlas
                      cluster.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  referenceHourOfDay:
                    description: UTC Hour of day between 0 and 23, inclusive, representing
                      which hour of the day that Atlas takes snapshots for backup
                      policy items.
                    type: number
                  referenceMinuteOfHour:
                    description: UTC Minutes after reference_hour_of_day that Atlas
                      takes snapshots for backup policy items. Must be between 0 and
                      59, inclusive.
                    type: number
                  restoreWindowDays:
                    description: Number of days back in time you can restore to with
                      point-in-time accuracy. Must be a positive, non-zero integer.
                    type: number
                  updateSnapshots:
                    description: Specify true to apply the retention changes in the
                      updated backup policy to snapshots that Atlas took previously.
                    type: boolean
                  useOrgAndGroupNamesInExportPrefix:
                    description: Specify true to use organization and project names
                      instead of organization and project UUIDs in the path for the
                      metadata files that Atlas uploads to your bucket after it finishes
                      exporting the snapshots. To learn more about the metadata files
                      that Atlas uploads, see Export Cloud Backup Snapshot.
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  autoExportEnabled:
                    description: 'Flag that indicates whether MongoDB Cloud automatically
                      exports Cloud Backup Snapshots to the Export Bucket. Value can
                      be one of the following:'
                    type: boolean
                  clusterName:
                    description: The name of the Atlas cluster that contains the snapshot
                      backup policy you want to retrieve.
                    type: string
                  copySettings:
                    description: List that contains a document for each copy setting
                      item in the desired backup policy. See below
                    items:
                      properties:
                        cloudProvider:
                          description: Human-readable label that identifies the cloud
                            provider that stores the snapshot copy. i.e. "AWS" "AZURE"
                            "GCP"
                          type: string
                        frequencies:
                          description: List that describes which types of snapshots
                            to copy. i.e. "HOURLY" "DAILY" "WEEKLY" "MONTHLY" "ON_DEMAND"
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        regionName:
                          description: Target region to copy snapshots belonging to
                            replicationSpecId to. Please supply the 'Atlas Region'
                            which can be found under https://www.mongodb.com/docs/atlas/reference/cloud-providers/
                            'regions' link
                          type: string
                        shouldCopyOplogs:
                          description: Flag that indicates whether to copy the oplogs
                            to the target region. You can use the oplogs to perform
                            point-in-time restores.
                          type: boolean
                        zoneId:
                          description: Unique 24-hexadecimal digit string that identifies
                            the zone in a cluster. For global clusters, there can
                            be multiple zones to choose from. For sharded clusters
                            and replica set clusters, there is only one zone in the
                            cluster. To find appropriate value for zone_id, do a GET
                            request to Return One Cluster from One Project and consult
                            the replicationSpecs array Return One Cluster From One
                            Project. Alternately, use mongodbatlas_advanced_cluster
                            data source or resource and reference replication_specs.#.zone_id.
                          type: string
                      type: object
                    type: array
                  export:
                    description: Policy for automatically exporting Cloud Backup Snapshots.
                      See below
                    items:
                      properties:
                        exportBucketId:
                          description: Unique identifier of the mongodbatlas_cloud_backup_snapshot_export_bucket
                            export_bucket_id value.
                          type: string
                        frequencyType:
                          description: 'Frequency associated with the export snapshot
                            item: weekly, monthly, yearly, daily (requires reaching
                            out to Customer Support)'
                          type: string
                      type: object
                    type: array
                  policyItemDaily:
                    description: Daily policy item. See below
                    items:
                      properties:
                        frequencyInterval:
                          description: Desired frequency of the new backup policy
                            item specified by frequency_type (daily in this case).
                            The only supported value for daily policies is 1 day.
                          type: number
                        retentionUnit:
                          description: 'Scope of the backup policy item: days, weeks,
                            months, or years.'
                          type: string
                        retentionValue:
                          description: 'Value to associate with retention_unit.  Note
                            that for less frequent policy items, Atlas requires that
                            you specify a retention period greater than or equal to
                            the retention period specified for more frequent policy
                            items. For example: If the hourly policy item specifies
                            a retention of two days, the daily retention policy must
                            specify two days or greater.'
                          type: number
                      type: object
                    type: array
                  policyItemHourly:
                    description: Hourly policy item. See below
                    items:
                      properties:
                        frequencyInterval:
                          description: Desired frequency of the new backup policy
                            item specified by frequency_type (hourly in this case).
                            The supported values for hourly policies are 1, 2, 4,
                            6, 8 or 12 hours. Note that 12 hours is the only accepted
                            value for NVMe clusters.
                          type: number
                        retentionUnit:
                          description: 'Scope of the backup policy item: days, weeks,
                            months, or years.'
                          type: string
                        retentionValue:
                          description: Value to associate with retention_unit.
                          type: number
                      type: object
                    type: array
                  policyItemMonthly:
                    description: Monthly policy item. See below
                    items:
                      properties:
                        frequencyInterval:
                          description: Desired frequency of the new backup policy
                            item specified by frequency_type (monthly in this case).
                            The supported values for weekly policies are
                          type: number
                        retentionUnit:
                          description: 'Scope of the backup policy item: days, weeks,
                            months, or years.'
                          type: string
                        retentionValue:
                          description: 'Value to associate with retention_unit. Monthly
                            policy must have retention days of at least 31 days or
                            5 weeks or 1 month. Note that for less frequent policy
                            items, Atlas requires that you specify a retention period
                            greater than or equal to the retention period specified
                            for more frequent policy items. For example: If the weekly
                            policy item specifies a retention of two weeks, the montly
                            retention policy must specify two weeks or greater.'
                          type: number
                      type: object
                    type: array
                  policyItemWeekly:
                    description: Weekly policy item. See below
                    items:
                      properties:
                        frequencyInterval:
                          description: Desired frequency of the new backup policy
                            item specified by frequency_type (weekly in this case).
                            The supported values for weekly policies are 1 through
                            7, where 1 represents Monday and 7 represents Sunday.
                          type: number
                        retentionUnit:
                          description: 'Scope of the backup policy item: days, weeks,
                            months, or years.'
                          type: string
                        retentionValue:
                          description: 'Value to associate with retention_unit. Weekly
                            policy must have retention of at least 7 days or 1 week.
                            Note that for less frequent policy items, Atlas requires
                            that you specify a retention period greater than or equal
                            to the retention period specified for more frequent policy
                            items. For example: If the daily policy item specifies
                            a retention of two weeks, the weekly retention policy
                            must specify two weeks or greater.'
                          type: number
                      type: object
                    type: array
                  policyItemYearly:
                    description: Yearly policy item. See below
                    items:
                      properties:
                        frequencyInterval:
                          description: Desired frequency of the new backup policy
                            item specified by frequency_type (yearly in this case).
                            The supported values for yearly policies are
                          type: number
                        retentionUnit:
                          description: 'Scope of the backup policy item: days, weeks,
                            months, or years.'
                          type: string
                        retentionValue:
                          description: Value to associate with retention_unit. Yearly
                            policy must have retention of at least 1 year.
                          type: number
                      type: object
                    type: array
                  projectId:
                    description: The unique identifier of the project for the Atlas
                      cluster.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  referenceHourOfDay:
                    description: UTC Hour of day between 0 and 23, inclusive, representing
                      which hour of the day that Atlas takes snapshots for backup
                      policy items.
                    type: number
                  referenceMinuteOfHour:
                    description: UTC Minutes after reference_hour_of_day that Atlas
                      takes snapshots for backup policy items. Must be between 0 and
                      59, inclusive.
                    type: number
                  restoreWindowDays:
                    description: Number of days back in time you can restore to with
                      point-in-time accuracy. Must be a positive, non-zero integer.
                    type: number
                  updateSnapshots:
                    description: Specify true to apply the retention changes in the
                      updated backup policy to snapshots that Atlas took previously.
                    type: boolean
                  useOrgAndGroupNamesInExportPrefix:
                    description: Specify true to use organization and project names
                      instead of organization and project UUIDs in the path for the
                      metadata files that Atlas uploads to your bucket after it finishes
                      exporting the snapshots. To learn more about the metadata files
                      that Atlas uploads, see Export Cloud Backup Snapshot.
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.clusterName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.clusterName)
                || (has(self.initProvider) && has(self.initProvider.clusterName))'
          status:
            description: BackupScheduleStatus defines the observed state of BackupSchedule.
            properties:
              atProvider:
                properties:
                  autoExportEnabled:
                    description: 'Flag that indicates whether MongoDB Cloud automatically
                      exports Cloud Backup Snapshots to the Export Bucket. Value can
                      be one of the following:'
                    type: boolean
                  clusterId:
                    description: Unique identifier of the Atlas cluster.
                    type: string
                  clusterName:
                    description: The name of the Atlas cluster that contains the snapshot
                      backup policy you want to retrieve.
                    type: string
                  copySettings:
                    description: List that contains a document for each copy setting
                      item in the desired backup policy. See below
                    items:
                      properties:
                        cloudProvider:
                          description: Human-readable label that identifies the cloud
                            provider that stores the snapshot copy. i.e. "AWS" "AZURE"
                            "GCP"
                          type: string
                        frequencies:
                          description: List that describes which types of snapshots
                            to copy. i.e. "HOURLY" "DAILY" "WEEKLY" "MONTHLY" "ON_DEMAND"
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: set
                        regionName:
                          description: Target region to copy snapshots belonging to
                            replicationSpecId to. Please supply the 'Atlas Region'
                            which can be found under https://www.mongodb.com/docs/atlas/reference/cloud-providers/
                            'regions' link
                          type: string
                        shouldCopyOplogs:
                          description: Flag that indicates whether to copy the oplogs
                            to the target region. You can use the oplogs to perform
                            point-in-time restores.
                          type: boolean
                        zoneId:
                          description: Unique 24-hexadecimal digit string that identifies
                            the zone in a cluster. For global clusters, there can
                            be multiple zones to choose from. For sharded clusters
                            and replica set clusters, there is only one zone in the
                            cluster. To find appropriate value for zone_id, do a GET
                            request to Return One Cluster from One Project and consult
                            the replicationSpecs array Return One Cluster From One
                            Project. Alternately, use mongodbatlas_advanced_cluster
                            data source or resource and reference replication_specs.#.zone_id.
                          type: string
                      type: object
                    type: array
                  export:
                    description: Policy for automatically exporting Cloud Backup Snapshots.
                      See below
                    items:
                      properties:
                        exportBucketId:
                          description: Unique identifier of the mongodbatlas_cloud_backup_snapshot_export_bucket
                            export_bucket_id value.
                          type: string
                        frequencyType:
                          description: 'Frequency associated with the export snapshot
                            item: weekly, monthly, yearly, daily (requires reaching
                            out to Customer Support)'
                          type: string
                      type: object
                    type: array
                  id:
                    description: Unique identifier of the backup policy item.
                    type: string
                  idPolicy:
                    description: Unique identifier of the backup policy.
                    type: string
                  nextSnapshot:
                    description: Timestamp in the number of seconds that have elapsed
                      since the UNIX epoch when Atlas takes the next snapshot.
                    type: string
                  policyItemDaily:
                    description: Daily policy item. See below
                    items:
                      properties:
                        frequencyInterval:
                          description: Desired frequency of the new backup policy
                            item specified by frequency_type (daily in this case).
                            The only supported value for daily policies is 1 day.
                          type: number
                        frequencyType:
                          description: Frequency associated with the backup policy
                            item. For daily policies, the frequency type is defined
                            as daily. Note that this is a read-only value and not
                            required in plan files - its value is implied from the
                            policy resource type.
                          type: string
                        id:
                          description: Unique identifier of the backup policy item.
                          type: string
                        retentionUnit:
                          description: 'Scope of the backup policy item: days, weeks,
                            months, or years.'
                          type: string
                        retentionValue:
                          description: 'Value to associate with retention_unit.  Note
                            that for less frequent policy items, Atlas requires that
                            you specify a retention period greater than or equal to
                            the retention period specified for more frequent policy
                            items. For example: If the hourly policy item specifies
                            a retention of two days, the daily retention policy must
                            specify two days or greater.'
                          type: number
                      type: object
                    type: array
                  policyItemHourly:
                    description: Hourly policy item. See below
                    items:
                      properties:
                        frequencyInterval:
                          description: Desired frequency of the new backup policy
                            item specified by frequency_type (hourly in this case).
                            The supported values for hourly policies are 1, 2, 4,
                            6, 8 or 12 hours. Note that 12 hours is the only accepted
                            value for NVMe clusters.
                          type: number
                        frequencyType:
                          description: Frequency associated with the backup policy
                            item. For hourly policies, the frequency type is defined
                            as hourly. Note that this is a read-only value and not
                            required in plan files - its value is implied from the
                            policy resource type.
                          type: string
                        id:
                          description: Unique identifier of the backup policy item.
                          type: string
                        retentionUnit:
                          description: 'Scope of the backup policy item: days, weeks,
                            months, or years.'
                          type: string
                        retentionValue:
                          description: Value to associate with retention_unit.
                          type: number
                      type: object
                    type: array
                  policyItemMonthly:
                    description: Monthly policy item. See below
                    items:
                      properties:
                        frequencyInterval:
                          description: Desired frequency of the new backup policy
                            item specified by frequency_type (monthly in this case).
                            The supported values for weekly policies are
                          type: number
                        frequencyType:
                          description: Frequency associated with the backup policy
                            item. For monthly policies, the frequency type is defined
                            as monthly. Note that this is a read-only value and not
                            required in plan files - its value is implied from the
                            policy resource type.
                          type: string
                        id:
                          description: Unique identifier of the backup policy item.
                          type: string
                        retentionUnit:
                          description: 'Scope of the backup policy item: days, weeks,
                            months, or years.'
                          type: string
                        retentionValue:
                          description: 'Value to associate with retention_unit. Monthly
                            policy must have retention days of at least 31 days or
                            5 weeks or 1 month. Note that for less frequent policy
                            items, Atlas requires that you specify a retention period
                            greater than or equal to the retention period specified
                            for more frequent policy items. For example: If the weekly
                            policy item specifies a retention of two weeks, the montly
                            retention policy must specify two weeks or greater.'
                          type: number
                      type: object
                    type: array
                  policyItemWeekly:
                    description: Weekly policy item. See below
                    items:
                      properties:
                        frequencyInterval:
                          description: Desired frequency of the new backup policy
                            item specified by frequency_type (weekly in this case).
                            The supported values for weekly policies are 1 through
                            7, where 1 represents Monday and 7 represents Sunday.
                          type: number
                        frequencyType:
                          description: Frequency associated with the backup policy
                            item. For weekly policies, the frequency type is defined
                            as weekly. Note that this is a read-only value and not
                            required in plan files - its value is implied from the
                            policy resource type.
                          type: string
                        id:
                          description: Unique identifier of the backup policy item.
                          type: string
                        retentionUnit:
                          description: 'Scope of the backup policy item: days, weeks,
                            months, or years.'
                          type: string
                        retentionValue:
                          description: 'Value to associate with retention_unit. Weekly
                            policy must have retention of at least 7 days or 1 week.
                            Note that for less frequent policy items, Atlas requires
                            that you specify a retention period greater than or equal
                            to the retention period specified for more frequent policy
                            items. For example: If the daily policy item specifies
                            a retention of two weeks, the weekly retention policy
                            must specify two weeks or greater.'
                          type: number
                      type: object
                    type: array
                  policyItemYearly:
                    description: Yearly policy item. See below
                    items:
                      properties:
                        frequencyInterval:
                          description: Desired frequency of the new backup policy
                            item specified by frequency_type (yearly in this case).
                            The supported values for yearly policies are
                          type: number
                        frequencyType:
                          description: Frequency associated with the backup policy
                            item. For yearly policies, the frequency type is defined
                            as yearly. Note that this is a read-only value and not
                            required in plan files - its value is implied from the
                            policy resource type.
                          type: string
                        id:
                          description: Unique identifier of the backup policy item.
                          type: string
                        retentionUnit:
                          description: 'Scope of the backup policy item: days, weeks,
                            months, or years.'
                          type: string
                        retentionValue:
                          description: Value to associate with retention_unit. Yearly
                            policy must have retention of at least 1 year.
                          type: number
                      type: object
                    type: array
                  projectId:
                    description: The unique identifier of the project for the Atlas
                      cluster.
                    type: string
                  referenceHourOfDay:
                    description: UTC Hour of day between 0 and 23, inclusive, representing
                      which hour of the day that Atlas takes snapshots for backup
                      policy items.
                    type: number
                  referenceMinuteOfHour:
                    description: UTC Minutes after reference_hour_of_day that Atlas
                      takes snapshots for backup policy items. Must be between 0 and
                      59, inclusive.
                    type: number
                  restoreWindowDays:
                    description: Number of days back in time you can restore to with
                      point-in-time accuracy. Must be a positive, non-zero integer.
                    type: number
                  updateSnapshots:
                    description: Specify true to apply the retention changes in the
                      updated backup policy to snapshots that Atlas took previously.
                    type: boolean
                  useOrgAndGroupNamesInExportPrefix:
                    description: Specify true to use organization and project names
                      instead of organization and project UUIDs in the path for the
                      metadata files that Atlas uploads to your bucket after it finishes
                      exporting the snapshots. To learn more about the metadata files
                      that Atlas uploads, see Export Cloud Backup Snapshot.
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
