---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: databaseinstances.federated.mongodbatlas.m.crossplane.io
spec:
  group: federated.mongodbatlas.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - mongodbatlas
    kind: DatabaseInstance
    listKind: DatabaseInstanceList
    plural: databaseinstances
    singular: databaseinstance
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DatabaseInstance is the Schema for the DatabaseInstances API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DatabaseInstanceSpec defines the desired state of DatabaseInstance
            properties:
              forProvider:
                properties:
                  cloudProviderConfig:
                    description: Cloud provider linked to this data federated instance.
                    items:
                      properties:
                        aws:
                          description: AWS provider of the cloud service where the
                            Federated Database Instance can access the S3 Bucket.
                          items:
                            properties:
                              roleId:
                                description: Unique identifier of the role that the
                                  Federated Instance can use to access the data stores.
                                  If necessary, use the Atlas UI or API to retrieve
                                  the role ID. You must also specify the test_s3_bucket.
                                type: string
                              testS3Bucket:
                                description: Name of the S3 data bucket that the provided
                                  role ID is authorized to access. You must also specify
                                  the role_id.
                                type: string
                            type: object
                          type: array
                        azure:
                          description: Microsoft Azure provider of the cloud service
                            where the Federated Database Instance can access Blob
                            Storage.
                          items:
                            properties:
                              roleId:
                                description: Unique identifier of the role that the
                                  Federated Database Instance can use to access the
                                  data stores.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  dataProcessRegion:
                    description: The cloud provider region to which the Federated
                      Instance routes client connections for data processing.
                    items:
                      properties:
                        cloudProvider:
                          description: 'Name of the cloud service provider. Supported
                            providers: AWS, AZURE.'
                          type: string
                        region:
                          description: Name of the region to which the Federated Instance
                            routes client connections for data processing. See the
                            documentation for the available region.
                          type: string
                      type: object
                    type: array
                  name:
                    description: Name of the Atlas Federated Database Instance.
                    type: string
                  projectId:
                    description: The unique ID for the project to create a Federated
                      Database Instance, also known as groupId in the official documentation.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  storageDatabases:
                    description: Configuration details for mapping each data store
                      to queryable databases and collections. For complete documentation
                      on this object and its nested fields, see databases. An empty
                      object indicates that the Federated Database Instance has no
                      mapping configuration for any data store.
                    items:
                      properties:
                        collections:
                          description: Array of objects where each object represents
                            a collection and data sources that map to a stores data
                            store.
                          items:
                            properties:
                              dataSources:
                                description: Array of objects where each object represents
                                  a stores data store to map with the collection.
                                items:
                                  properties:
                                    allowInsecure:
                                      description: Flag that validates the scheme
                                        in the specified URLs.
                                      type: boolean
                                    collection:
                                      description: Human-readable label that identifies
                                        the collection in the database.
                                      type: string
                                    collectionRegex:
                                      description: Regex pattern to use for creating
                                        the wildcard (*) collection.
                                      type: string
                                    database:
                                      description: Human-readable label that identifies
                                        the database, which contains the collection
                                        in the cluster.
                                      type: string
                                    databaseRegex:
                                      description: Regex pattern to use for creating
                                        the wildcard database.
                                      type: string
                                    datasetName:
                                      description: Human-readable label that identifies
                                        the dataset that Atlas generates for an ingestion
                                        pipeline run or Online Archive.
                                      type: string
                                    defaultFormat:
                                      description: Default format that Data Federation
                                        assumes if it encounters a file without an
                                        extension while searching the storeName.
                                      type: string
                                    path:
                                      description: File path that controls how MongoDB
                                        Cloud searches for and parses files in the
                                        storeName before mapping them to a collection.
                                        Specify / to capture all files and folders
                                        from the prefix path.
                                      type: string
                                    provenanceFieldName:
                                      description: Name for the field that includes
                                        the provenance of the documents in the results.
                                      type: string
                                    storeName:
                                      description: Name of a data store to map to
                                        the <collection>. Must match the name of an
                                        object in the stores array.
                                      type: string
                                    urls:
                                      description: Comma-separated list of publicly
                                        accessible HTTP URLs where data is stored.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                              name:
                                description: Human-readable label of the tag.
                                type: string
                            type: object
                          type: array
                        name:
                          description: Human-readable label of the tag.
                          type: string
                        views:
                          description: Array of objects where each object represents
                            an aggregation pipeline on a collection. To learn more
                            about views, see Views.
                          items:
                            type: object
                          type: array
                      type: object
                    type: array
                  storageStores:
                    description: Each object in the array represents a data store.
                      Federated Database uses the storage.databases configuration
                      details to map data in each data store to queryable databases
                      and collections. For complete documentation on this object and
                      its nested fields, see stores. An empty object indicates that
                      the Federated Database Instance has no configured data stores.
                    items:
                      properties:
                        additionalStorageClasses:
                          items:
                            type: string
                          type: array
                        allowInsecure:
                          description: Flag that validates the scheme in the specified
                            URLs.
                          type: boolean
                        bucket:
                          description: Name of the AWS S3 bucket.
                          type: string
                        clusterName:
                          description: Human-readable label of the MongoDB Cloud cluster
                            on which the store is based.
                          type: string
                        defaultFormat:
                          description: Default format that Data Federation assumes
                            if it encounters a file without an extension while searching
                            the storeName.
                          type: string
                        delimiter:
                          description: The delimiter that separates storage_databases.#.collections.#.data_sources.#.path
                            segments in the data store.
                          type: string
                        includeTags:
                          description: Determines whether or not to use S3 tags on
                            the files in the given path as additional partition attributes.
                          type: boolean
                        name:
                          description: Human-readable label of the tag.
                          type: string
                        prefix:
                          description: Prefix the Federated Database Instance applies
                            when searching for files in the S3 bucket.
                          type: string
                        projectId:
                          description: The unique ID for the project to create a Federated
                            Database Instance, also known as groupId in the official
                            documentation.
                          type: string
                        provider:
                          description: Defines where the data is stored.
                          type: string
                        public:
                          description: Flag that indicates whether the bucket is public.
                          type: string
                        readPreference:
                          description: MongoDB Cloud cluster read preference, which
                            describes how to route read requests to the cluster.
                          items:
                            properties:
                              maxStalenessSeconds:
                                type: number
                              mode:
                                description: Read preference mode that specifies to
                                  which replica set member to route the read requests.
                                type: string
                              tagSets:
                                description: List that contains tag sets or tag specification
                                  documents.
                                items:
                                  properties:
                                    tags:
                                      description: List of all tags within a tag set
                                      items:
                                        properties:
                                          name:
                                            description: Human-readable label of the
                                              tag.
                                            type: string
                                          value:
                                            description: Value of the tag.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        region:
                          description: Name of the AWS region in which the S3 bucket
                            is hosted.
                          type: string
                        urls:
                          description: Comma-separated list of publicly accessible
                            HTTP URLs where data is stored.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  cloudProviderConfig:
                    description: Cloud provider linked to this data federated instance.
                    items:
                      properties:
                        aws:
                          description: AWS provider of the cloud service where the
                            Federated Database Instance can access the S3 Bucket.
                          items:
                            properties:
                              roleId:
                                description: Unique identifier of the role that the
                                  Federated Instance can use to access the data stores.
                                  If necessary, use the Atlas UI or API to retrieve
                                  the role ID. You must also specify the test_s3_bucket.
                                type: string
                              testS3Bucket:
                                description: Name of the S3 data bucket that the provided
                                  role ID is authorized to access. You must also specify
                                  the role_id.
                                type: string
                            type: object
                          type: array
                        azure:
                          description: Microsoft Azure provider of the cloud service
                            where the Federated Database Instance can access Blob
                            Storage.
                          items:
                            properties:
                              roleId:
                                description: Unique identifier of the role that the
                                  Federated Database Instance can use to access the
                                  data stores.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  dataProcessRegion:
                    description: The cloud provider region to which the Federated
                      Instance routes client connections for data processing.
                    items:
                      properties:
                        cloudProvider:
                          description: 'Name of the cloud service provider. Supported
                            providers: AWS, AZURE.'
                          type: string
                        region:
                          description: Name of the region to which the Federated Instance
                            routes client connections for data processing. See the
                            documentation for the available region.
                          type: string
                      type: object
                    type: array
                  name:
                    description: Name of the Atlas Federated Database Instance.
                    type: string
                  projectId:
                    description: The unique ID for the project to create a Federated
                      Database Instance, also known as groupId in the official documentation.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  storageDatabases:
                    description: Configuration details for mapping each data store
                      to queryable databases and collections. For complete documentation
                      on this object and its nested fields, see databases. An empty
                      object indicates that the Federated Database Instance has no
                      mapping configuration for any data store.
                    items:
                      properties:
                        collections:
                          description: Array of objects where each object represents
                            a collection and data sources that map to a stores data
                            store.
                          items:
                            properties:
                              dataSources:
                                description: Array of objects where each object represents
                                  a stores data store to map with the collection.
                                items:
                                  properties:
                                    allowInsecure:
                                      description: Flag that validates the scheme
                                        in the specified URLs.
                                      type: boolean
                                    collection:
                                      description: Human-readable label that identifies
                                        the collection in the database.
                                      type: string
                                    collectionRegex:
                                      description: Regex pattern to use for creating
                                        the wildcard (*) collection.
                                      type: string
                                    database:
                                      description: Human-readable label that identifies
                                        the database, which contains the collection
                                        in the cluster.
                                      type: string
                                    databaseRegex:
                                      description: Regex pattern to use for creating
                                        the wildcard database.
                                      type: string
                                    datasetName:
                                      description: Human-readable label that identifies
                                        the dataset that Atlas generates for an ingestion
                                        pipeline run or Online Archive.
                                      type: string
                                    defaultFormat:
                                      description: Default format that Data Federation
                                        assumes if it encounters a file without an
                                        extension while searching the storeName.
                                      type: string
                                    path:
                                      description: File path that controls how MongoDB
                                        Cloud searches for and parses files in the
                                        storeName before mapping them to a collection.
                                        Specify / to capture all files and folders
                                        from the prefix path.
                                      type: string
                                    provenanceFieldName:
                                      description: Name for the field that includes
                                        the provenance of the documents in the results.
                                      type: string
                                    storeName:
                                      description: Name of a data store to map to
                                        the <collection>. Must match the name of an
                                        object in the stores array.
                                      type: string
                                    urls:
                                      description: Comma-separated list of publicly
                                        accessible HTTP URLs where data is stored.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                              name:
                                description: Human-readable label of the tag.
                                type: string
                            type: object
                          type: array
                        name:
                          description: Human-readable label of the tag.
                          type: string
                        views:
                          description: Array of objects where each object represents
                            an aggregation pipeline on a collection. To learn more
                            about views, see Views.
                          items:
                            type: object
                          type: array
                      type: object
                    type: array
                  storageStores:
                    description: Each object in the array represents a data store.
                      Federated Database uses the storage.databases configuration
                      details to map data in each data store to queryable databases
                      and collections. For complete documentation on this object and
                      its nested fields, see stores. An empty object indicates that
                      the Federated Database Instance has no configured data stores.
                    items:
                      properties:
                        additionalStorageClasses:
                          items:
                            type: string
                          type: array
                        allowInsecure:
                          description: Flag that validates the scheme in the specified
                            URLs.
                          type: boolean
                        bucket:
                          description: Name of the AWS S3 bucket.
                          type: string
                        clusterName:
                          description: Human-readable label of the MongoDB Cloud cluster
                            on which the store is based.
                          type: string
                        defaultFormat:
                          description: Default format that Data Federation assumes
                            if it encounters a file without an extension while searching
                            the storeName.
                          type: string
                        delimiter:
                          description: The delimiter that separates storage_databases.#.collections.#.data_sources.#.path
                            segments in the data store.
                          type: string
                        includeTags:
                          description: Determines whether or not to use S3 tags on
                            the files in the given path as additional partition attributes.
                          type: boolean
                        name:
                          description: Human-readable label of the tag.
                          type: string
                        prefix:
                          description: Prefix the Federated Database Instance applies
                            when searching for files in the S3 bucket.
                          type: string
                        projectId:
                          description: The unique ID for the project to create a Federated
                            Database Instance, also known as groupId in the official
                            documentation.
                          type: string
                        provider:
                          description: Defines where the data is stored.
                          type: string
                        public:
                          description: Flag that indicates whether the bucket is public.
                          type: string
                        readPreference:
                          description: MongoDB Cloud cluster read preference, which
                            describes how to route read requests to the cluster.
                          items:
                            properties:
                              maxStalenessSeconds:
                                type: number
                              mode:
                                description: Read preference mode that specifies to
                                  which replica set member to route the read requests.
                                type: string
                              tagSets:
                                description: List that contains tag sets or tag specification
                                  documents.
                                items:
                                  properties:
                                    tags:
                                      description: List of all tags within a tag set
                                      items:
                                        properties:
                                          name:
                                            description: Human-readable label of the
                                              tag.
                                            type: string
                                          value:
                                            description: Value of the tag.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        region:
                          description: Name of the AWS region in which the S3 bucket
                            is hosted.
                          type: string
                        urls:
                          description: Comma-separated list of publicly accessible
                            HTTP URLs where data is stored.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: DatabaseInstanceStatus defines the observed state of DatabaseInstance.
            properties:
              atProvider:
                properties:
                  cloudProviderConfig:
                    description: Cloud provider linked to this data federated instance.
                    items:
                      properties:
                        aws:
                          description: AWS provider of the cloud service where the
                            Federated Database Instance can access the S3 Bucket.
                          items:
                            properties:
                              externalId:
                                type: string
                              iamAssumedRoleArn:
                                description: 'Amazon Resource Name (ARN) of the IAM
                                  Role that the Federated Database Instance assumes
                                  when accessing S3 Bucket data stores. The IAM Role
                                  must support the following actions against each
                                  S3 bucket:'
                                type: string
                              iamUserArn:
                                description: |-
                                  Amazon Resource Name (ARN) of the user that the Federated Database Instance assumes when accessing S3 Bucket data stores.
                                  * external_id - Unique identifier associated with the IAM Role that the Federated Database Instance assumes when accessing the data stores.
                                type: string
                              roleId:
                                description: Unique identifier of the role that the
                                  Federated Instance can use to access the data stores.
                                  If necessary, use the Atlas UI or API to retrieve
                                  the role ID. You must also specify the test_s3_bucket.
                                type: string
                              testS3Bucket:
                                description: Name of the S3 data bucket that the provided
                                  role ID is authorized to access. You must also specify
                                  the role_id.
                                type: string
                            type: object
                          type: array
                        azure:
                          description: Microsoft Azure provider of the cloud service
                            where the Federated Database Instance can access Blob
                            Storage.
                          items:
                            properties:
                              atlasAppId:
                                type: string
                              roleId:
                                description: Unique identifier of the role that the
                                  Federated Database Instance can use to access the
                                  data stores.
                                type: string
                              servicePrincipalId:
                                description: Unique identifier of the Azure service
                                  principal that the Federated Database instance uses
                                  to access Azure Blob Storage.
                                type: string
                              tenantId:
                                description: Unique identifier of the Azure Active
                                  Directory tenant where the service principal resides.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  dataProcessRegion:
                    description: The cloud provider region to which the Federated
                      Instance routes client connections for data processing.
                    items:
                      properties:
                        cloudProvider:
                          description: 'Name of the cloud service provider. Supported
                            providers: AWS, AZURE.'
                          type: string
                        region:
                          description: Name of the region to which the Federated Instance
                            routes client connections for data processing. See the
                            documentation for the available region.
                          type: string
                      type: object
                    type: array
                  hostnames:
                    description: The list of hostnames assigned to the Federated Database
                      Instance. Each string in the array is a hostname assigned to
                      the Federated Database Instance.
                    items:
                      type: string
                    type: array
                  id:
                    type: string
                  name:
                    description: Name of the Atlas Federated Database Instance.
                    type: string
                  projectId:
                    description: The unique ID for the project to create a Federated
                      Database Instance, also known as groupId in the official documentation.
                    type: string
                  state:
                    description: 'Current state of the Federated Database Instance:'
                    type: string
                  storageDatabases:
                    description: Configuration details for mapping each data store
                      to queryable databases and collections. For complete documentation
                      on this object and its nested fields, see databases. An empty
                      object indicates that the Federated Database Instance has no
                      mapping configuration for any data store.
                    items:
                      properties:
                        collections:
                          description: Array of objects where each object represents
                            a collection and data sources that map to a stores data
                            store.
                          items:
                            properties:
                              dataSources:
                                description: Array of objects where each object represents
                                  a stores data store to map with the collection.
                                items:
                                  properties:
                                    allowInsecure:
                                      description: Flag that validates the scheme
                                        in the specified URLs.
                                      type: boolean
                                    collection:
                                      description: Human-readable label that identifies
                                        the collection in the database.
                                      type: string
                                    collectionRegex:
                                      description: Regex pattern to use for creating
                                        the wildcard (*) collection.
                                      type: string
                                    database:
                                      description: Human-readable label that identifies
                                        the database, which contains the collection
                                        in the cluster.
                                      type: string
                                    databaseRegex:
                                      description: Regex pattern to use for creating
                                        the wildcard database.
                                      type: string
                                    datasetName:
                                      description: Human-readable label that identifies
                                        the dataset that Atlas generates for an ingestion
                                        pipeline run or Online Archive.
                                      type: string
                                    defaultFormat:
                                      description: Default format that Data Federation
                                        assumes if it encounters a file without an
                                        extension while searching the storeName.
                                      type: string
                                    path:
                                      description: File path that controls how MongoDB
                                        Cloud searches for and parses files in the
                                        storeName before mapping them to a collection.
                                        Specify / to capture all files and folders
                                        from the prefix path.
                                      type: string
                                    provenanceFieldName:
                                      description: Name for the field that includes
                                        the provenance of the documents in the results.
                                      type: string
                                    storeName:
                                      description: Name of a data store to map to
                                        the <collection>. Must match the name of an
                                        object in the stores array.
                                      type: string
                                    urls:
                                      description: Comma-separated list of publicly
                                        accessible HTTP URLs where data is stored.
                                      items:
                                        type: string
                                      type: array
                                  type: object
                                type: array
                              name:
                                description: Human-readable label of the tag.
                                type: string
                            type: object
                          type: array
                        maxWildcardCollections:
                          type: number
                        name:
                          description: Human-readable label of the tag.
                          type: string
                        views:
                          description: Array of objects where each object represents
                            an aggregation pipeline on a collection. To learn more
                            about views, see Views.
                          items:
                            properties:
                              name:
                                description: Human-readable label of the tag.
                                type: string
                              pipeline:
                                description: Aggregation pipeline stage(s) to apply
                                  to the source collection.
                                type: string
                              source:
                                description: Name of the source collection for the
                                  view.
                                type: string
                            type: object
                          type: array
                      type: object
                    type: array
                  storageStores:
                    description: Each object in the array represents a data store.
                      Federated Database uses the storage.databases configuration
                      details to map data in each data store to queryable databases
                      and collections. For complete documentation on this object and
                      its nested fields, see stores. An empty object indicates that
                      the Federated Database Instance has no configured data stores.
                    items:
                      properties:
                        additionalStorageClasses:
                          items:
                            type: string
                          type: array
                        allowInsecure:
                          description: Flag that validates the scheme in the specified
                            URLs.
                          type: boolean
                        bucket:
                          description: Name of the AWS S3 bucket.
                          type: string
                        clusterName:
                          description: Human-readable label of the MongoDB Cloud cluster
                            on which the store is based.
                          type: string
                        defaultFormat:
                          description: Default format that Data Federation assumes
                            if it encounters a file without an extension while searching
                            the storeName.
                          type: string
                        delimiter:
                          description: The delimiter that separates storage_databases.#.collections.#.data_sources.#.path
                            segments in the data store.
                          type: string
                        includeTags:
                          description: Determines whether or not to use S3 tags on
                            the files in the given path as additional partition attributes.
                          type: boolean
                        name:
                          description: Human-readable label of the tag.
                          type: string
                        prefix:
                          description: Prefix the Federated Database Instance applies
                            when searching for files in the S3 bucket.
                          type: string
                        projectId:
                          description: The unique ID for the project to create a Federated
                            Database Instance, also known as groupId in the official
                            documentation.
                          type: string
                        provider:
                          description: Defines where the data is stored.
                          type: string
                        public:
                          description: Flag that indicates whether the bucket is public.
                          type: string
                        readPreference:
                          description: MongoDB Cloud cluster read preference, which
                            describes how to route read requests to the cluster.
                          items:
                            properties:
                              maxStalenessSeconds:
                                type: number
                              mode:
                                description: Read preference mode that specifies to
                                  which replica set member to route the read requests.
                                type: string
                              tagSets:
                                description: List that contains tag sets or tag specification
                                  documents.
                                items:
                                  properties:
                                    tags:
                                      description: List of all tags within a tag set
                                      items:
                                        properties:
                                          name:
                                            description: Human-readable label of the
                                              tag.
                                            type: string
                                          value:
                                            description: Value of the tag.
                                            type: string
                                        type: object
                                      type: array
                                  type: object
                                type: array
                            type: object
                          type: array
                        region:
                          description: Name of the AWS region in which the S3 bucket
                            is hosted.
                          type: string
                        urls:
                          description: Comma-separated list of publicly accessible
                            HTTP URLs where data is stored.
                          items:
                            type: string
                          type: array
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
