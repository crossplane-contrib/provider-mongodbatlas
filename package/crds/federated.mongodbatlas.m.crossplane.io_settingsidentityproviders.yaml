---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: settingsidentityproviders.federated.mongodbatlas.m.crossplane.io
spec:
  group: federated.mongodbatlas.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - mongodbatlas
    kind: SettingsIdentityProvider
    listKind: SettingsIdentityProviderList
    plural: settingsidentityproviders
    singular: settingsidentityprovider
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SettingsIdentityProvider is the Schema for the SettingsIdentityProviders
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SettingsIdentityProviderSpec defines the desired state of
              SettingsIdentityProvider
            properties:
              forProvider:
                properties:
                  associatedDomains:
                    description: List that contains the domains associated with the
                      identity provider.
                    items:
                      type: string
                    type: array
                  audience:
                    description: Identifier of the intended recipient of the token
                      used in OIDC IdP.
                    type: string
                  authorizationType:
                    description: Indicates whether authorization is granted based
                      on group membership or user ID. Valid values are GROUP or USER.
                    type: string
                  clientId:
                    description: Client identifier that is assigned to an application
                      by the OIDC Identity Provider.
                    type: string
                  description:
                    description: The description of the identity provider.
                    type: string
                  federationSettingsId:
                    description: Unique 24-hexadecimal digit string that identifies
                      the federated authentication configuration.
                    type: string
                  groupsClaim:
                    description: Identifier of the claim which contains OIDC IdP Group
                      IDs in the token.
                    type: string
                  idpType:
                    type: string
                  issuerUri:
                    description: Unique string that identifies the issuer of the IdP.
                    type: string
                  name:
                    description: Human-readable label that identifies the identity
                      provider.
                    type: string
                  protocol:
                    description: The protocol of the identity provider. Either SAML
                      or OIDC.
                    type: string
                  requestBinding:
                    description: 'SAML Authentication Request Protocol HTTP method
                      binding (POST or REDIRECT) that Federated Authentication uses
                      to send the authentication request. Atlas supports the following
                      binding values:'
                    type: string
                  requestedScopes:
                    description: Scopes that MongoDB applications will request from
                      the authorization endpoint used for OIDC IdPs.
                    items:
                      type: string
                    type: array
                  responseSignatureAlgorithm:
                    description: Signature algorithm that Federated Authentication
                      uses to encrypt the identity provider signature.  Valid values
                      include SHA-1 and SHA-256.
                    type: string
                  ssoDebugEnabled:
                    description: Flag that indicates whether the identity provider
                      has SSO debug enabled.
                    type: boolean
                  ssoUrl:
                    description: Unique string that identifies the intended audience
                      of the SAML assertion.
                    type: string
                  status:
                    description: String enum that indicates whether the identity provider
                      is active or not. Accepted values are ACTIVE or INACTIVE.
                    type: string
                  userClaim:
                    description: |-
                      Identifier of the claim which contains the user ID in the token used for OIDC IdPs.
                      userClaim is required for OIDC IdP with authorizationType GROUP and USER.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  associatedDomains:
                    description: List that contains the domains associated with the
                      identity provider.
                    items:
                      type: string
                    type: array
                  audience:
                    description: Identifier of the intended recipient of the token
                      used in OIDC IdP.
                    type: string
                  authorizationType:
                    description: Indicates whether authorization is granted based
                      on group membership or user ID. Valid values are GROUP or USER.
                    type: string
                  clientId:
                    description: Client identifier that is assigned to an application
                      by the OIDC Identity Provider.
                    type: string
                  description:
                    description: The description of the identity provider.
                    type: string
                  federationSettingsId:
                    description: Unique 24-hexadecimal digit string that identifies
                      the federated authentication configuration.
                    type: string
                  groupsClaim:
                    description: Identifier of the claim which contains OIDC IdP Group
                      IDs in the token.
                    type: string
                  idpType:
                    type: string
                  issuerUri:
                    description: Unique string that identifies the issuer of the IdP.
                    type: string
                  name:
                    description: Human-readable label that identifies the identity
                      provider.
                    type: string
                  protocol:
                    description: The protocol of the identity provider. Either SAML
                      or OIDC.
                    type: string
                  requestBinding:
                    description: 'SAML Authentication Request Protocol HTTP method
                      binding (POST or REDIRECT) that Federated Authentication uses
                      to send the authentication request. Atlas supports the following
                      binding values:'
                    type: string
                  requestedScopes:
                    description: Scopes that MongoDB applications will request from
                      the authorization endpoint used for OIDC IdPs.
                    items:
                      type: string
                    type: array
                  responseSignatureAlgorithm:
                    description: Signature algorithm that Federated Authentication
                      uses to encrypt the identity provider signature.  Valid values
                      include SHA-1 and SHA-256.
                    type: string
                  ssoDebugEnabled:
                    description: Flag that indicates whether the identity provider
                      has SSO debug enabled.
                    type: boolean
                  ssoUrl:
                    description: Unique string that identifies the intended audience
                      of the SAML assertion.
                    type: string
                  status:
                    description: String enum that indicates whether the identity provider
                      is active or not. Accepted values are ACTIVE or INACTIVE.
                    type: string
                  userClaim:
                    description: |-
                      Identifier of the claim which contains the user ID in the token used for OIDC IdPs.
                      userClaim is required for OIDC IdP with authorizationType GROUP and USER.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.federationSettingsId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.federationSettingsId)
                || (has(self.initProvider) && has(self.initProvider.federationSettingsId))'
            - message: spec.forProvider.issuerUri is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.issuerUri)
                || (has(self.initProvider) && has(self.initProvider.issuerUri))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: SettingsIdentityProviderStatus defines the observed state
              of SettingsIdentityProvider.
            properties:
              atProvider:
                properties:
                  associatedDomains:
                    description: List that contains the domains associated with the
                      identity provider.
                    items:
                      type: string
                    type: array
                  audience:
                    description: Identifier of the intended recipient of the token
                      used in OIDC IdP.
                    type: string
                  authorizationType:
                    description: Indicates whether authorization is granted based
                      on group membership or user ID. Valid values are GROUP or USER.
                    type: string
                  clientId:
                    description: Client identifier that is assigned to an application
                      by the OIDC Identity Provider.
                    type: string
                  description:
                    description: The description of the identity provider.
                    type: string
                  federationSettingsId:
                    description: Unique 24-hexadecimal digit string that identifies
                      the federated authentication configuration.
                    type: string
                  groupsClaim:
                    description: Identifier of the claim which contains OIDC IdP Group
                      IDs in the token.
                    type: string
                  id:
                    type: string
                  idpId:
                    description: Unique 24-hexadecimal digit string that identifies
                      the IdP.
                    type: string
                  idpType:
                    type: string
                  issuerUri:
                    description: Unique string that identifies the issuer of the IdP.
                    type: string
                  name:
                    description: Human-readable label that identifies the identity
                      provider.
                    type: string
                  oktaIdpId:
                    description: Unique 20-hexadecimal digit string that identifies
                      the IdP.
                    type: string
                  protocol:
                    description: The protocol of the identity provider. Either SAML
                      or OIDC.
                    type: string
                  requestBinding:
                    description: 'SAML Authentication Request Protocol HTTP method
                      binding (POST or REDIRECT) that Federated Authentication uses
                      to send the authentication request. Atlas supports the following
                      binding values:'
                    type: string
                  requestedScopes:
                    description: Scopes that MongoDB applications will request from
                      the authorization endpoint used for OIDC IdPs.
                    items:
                      type: string
                    type: array
                  responseSignatureAlgorithm:
                    description: Signature algorithm that Federated Authentication
                      uses to encrypt the identity provider signature.  Valid values
                      include SHA-1 and SHA-256.
                    type: string
                  ssoDebugEnabled:
                    description: Flag that indicates whether the identity provider
                      has SSO debug enabled.
                    type: boolean
                  ssoUrl:
                    description: Unique string that identifies the intended audience
                      of the SAML assertion.
                    type: string
                  status:
                    description: String enum that indicates whether the identity provider
                      is active or not. Accepted values are ACTIVE or INACTIVE.
                    type: string
                  userClaim:
                    description: |-
                      Identifier of the claim which contains the user ID in the token used for OIDC IdPs.
                      userClaim is required for OIDC IdP with authorizationType GROUP and USER.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
