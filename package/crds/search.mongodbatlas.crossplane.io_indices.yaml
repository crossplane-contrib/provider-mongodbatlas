---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: indices.search.mongodbatlas.crossplane.io
spec:
  group: search.mongodbatlas.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - mongodbatlas
    kind: Index
    listKind: IndexList
    plural: indices
    singular: index
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Index is the Schema for the Indexs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: IndexSpec defines the desired state of Index
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  analyzer:
                    description: Analyzer to use when creating the index. Defaults
                      to lucene.standard
                    type: string
                  analyzers:
                    description: Custom analyzers to use in this index. This is an
                      array of JSON objects.
                    type: string
                  clusterName:
                    description: The name of the cluster where you want to create
                      the search index within.
                    type: string
                  collectionName:
                    description: 'Name of the collection the index is on. NOTE: The
                      collection must exist before creating the index.'
                    type: string
                  database:
                    description: Name of the database the collection is in.
                    type: string
                  fields:
                    description: Array of Fields to configure this vectorSearch index.
                      It is mandatory for vector searches and it must contain at least
                      one vector type field. This field needs to be a JSON string
                      in order to be decoded correctly.
                    type: string
                  mappingsDynamic:
                    description: Indicates whether the search index uses dynamic or
                      static mapping. For default dynamic mapping, set the value to
                      true. For static mapping, specify the fields to index using
                      mappings_fields. Mutually exclusive with mappings_dynamic_config.
                    type: boolean
                  mappingsDynamicConfig:
                    description: JSON object for mappings.dynamic when using configurable
                      dynamic. See the MongoDB documentation for further information
                      on Static and Dynamic Mapping. Mutually exclusive with mappings_dynamic.
                    type: string
                  mappingsFields:
                    description: attribute is required in search indexes when mappings_dynamic
                      is false. This field needs to be a JSON string in order to be
                      decoded correctly.
                    type: string
                  name:
                    description: The name of the search index you want to create.
                    type: string
                  numPartitions:
                    description: Number of index partitions. Allowed values are [1,
                      2, 4]. Default value is 1.
                    type: number
                  projectId:
                    description: The ID of the organization or project you want to
                      create the search index within.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  searchAnalyzer:
                    description: Analyzer to use when searching the index. Defaults
                      to lucene.standard
                    type: string
                  storedSource:
                    description: String that can be "true" (store all fields), "false"
                      (default, don't store any field), or a JSON string that contains
                      the list of fields to store (include) or not store (exclude)
                      on Atlas Search. To learn more, see Stored Source Fields.
                    type: string
                  synonyms:
                    description: Synonyms mapping definition to use in this index.
                    items:
                      properties:
                        analyzer:
                          description: Analyzer to use when creating the index. Defaults
                            to lucene.standard
                          type: string
                        name:
                          description: Name of the type set.
                          type: string
                        sourceCollection:
                          description: Name of the source MongoDB collection for the
                            synonyms. Documents in this collection must be in the
                            format described in the Synonyms Source Collection Documents.
                          type: string
                      type: object
                    type: array
                  type:
                    description: 'Type of index: search or vectorSearch. Default type
                      is search.'
                    type: string
                  typeSets:
                    description: One or more blocks defining configurable dynamic
                      type sets. Atlas only persists/returns typeSets when mappings.dynamic
                      is an object referencing a typeSet name.
                    items:
                      properties:
                        name:
                          description: Name of the type set.
                          type: string
                        types:
                          description: JSON array describing the types.
                          type: string
                      type: object
                    type: array
                  waitForIndexBuildCompletion:
                    type: boolean
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  analyzer:
                    description: Analyzer to use when creating the index. Defaults
                      to lucene.standard
                    type: string
                  analyzers:
                    description: Custom analyzers to use in this index. This is an
                      array of JSON objects.
                    type: string
                  clusterName:
                    description: The name of the cluster where you want to create
                      the search index within.
                    type: string
                  collectionName:
                    description: 'Name of the collection the index is on. NOTE: The
                      collection must exist before creating the index.'
                    type: string
                  database:
                    description: Name of the database the collection is in.
                    type: string
                  fields:
                    description: Array of Fields to configure this vectorSearch index.
                      It is mandatory for vector searches and it must contain at least
                      one vector type field. This field needs to be a JSON string
                      in order to be decoded correctly.
                    type: string
                  mappingsDynamic:
                    description: Indicates whether the search index uses dynamic or
                      static mapping. For default dynamic mapping, set the value to
                      true. For static mapping, specify the fields to index using
                      mappings_fields. Mutually exclusive with mappings_dynamic_config.
                    type: boolean
                  mappingsDynamicConfig:
                    description: JSON object for mappings.dynamic when using configurable
                      dynamic. See the MongoDB documentation for further information
                      on Static and Dynamic Mapping. Mutually exclusive with mappings_dynamic.
                    type: string
                  mappingsFields:
                    description: attribute is required in search indexes when mappings_dynamic
                      is false. This field needs to be a JSON string in order to be
                      decoded correctly.
                    type: string
                  name:
                    description: The name of the search index you want to create.
                    type: string
                  numPartitions:
                    description: Number of index partitions. Allowed values are [1,
                      2, 4]. Default value is 1.
                    type: number
                  projectId:
                    description: The ID of the organization or project you want to
                      create the search index within.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  searchAnalyzer:
                    description: Analyzer to use when searching the index. Defaults
                      to lucene.standard
                    type: string
                  storedSource:
                    description: String that can be "true" (store all fields), "false"
                      (default, don't store any field), or a JSON string that contains
                      the list of fields to store (include) or not store (exclude)
                      on Atlas Search. To learn more, see Stored Source Fields.
                    type: string
                  synonyms:
                    description: Synonyms mapping definition to use in this index.
                    items:
                      properties:
                        analyzer:
                          description: Analyzer to use when creating the index. Defaults
                            to lucene.standard
                          type: string
                        name:
                          description: Name of the type set.
                          type: string
                        sourceCollection:
                          description: Name of the source MongoDB collection for the
                            synonyms. Documents in this collection must be in the
                            format described in the Synonyms Source Collection Documents.
                          type: string
                      type: object
                    type: array
                  type:
                    description: 'Type of index: search or vectorSearch. Default type
                      is search.'
                    type: string
                  typeSets:
                    description: One or more blocks defining configurable dynamic
                      type sets. Atlas only persists/returns typeSets when mappings.dynamic
                      is an object referencing a typeSet name.
                    items:
                      properties:
                        name:
                          description: Name of the type set.
                          type: string
                        types:
                          description: JSON array describing the types.
                          type: string
                      type: object
                    type: array
                  waitForIndexBuildCompletion:
                    type: boolean
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.clusterName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.clusterName)
                || (has(self.initProvider) && has(self.initProvider.clusterName))'
            - message: spec.forProvider.collectionName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.collectionName)
                || (has(self.initProvider) && has(self.initProvider.collectionName))'
            - message: spec.forProvider.database is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.database)
                || (has(self.initProvider) && has(self.initProvider.database))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: IndexStatus defines the observed state of Index.
            properties:
              atProvider:
                properties:
                  analyzer:
                    description: Analyzer to use when creating the index. Defaults
                      to lucene.standard
                    type: string
                  analyzers:
                    description: Custom analyzers to use in this index. This is an
                      array of JSON objects.
                    type: string
                  clusterName:
                    description: The name of the cluster where you want to create
                      the search index within.
                    type: string
                  collectionName:
                    description: 'Name of the collection the index is on. NOTE: The
                      collection must exist before creating the index.'
                    type: string
                  database:
                    description: Name of the database the collection is in.
                    type: string
                  fields:
                    description: Array of Fields to configure this vectorSearch index.
                      It is mandatory for vector searches and it must contain at least
                      one vector type field. This field needs to be a JSON string
                      in order to be decoded correctly.
                    type: string
                  id:
                    type: string
                  indexId:
                    description: The unique identifier of the Atlas Search index.
                    type: string
                  mappingsDynamic:
                    description: Indicates whether the search index uses dynamic or
                      static mapping. For default dynamic mapping, set the value to
                      true. For static mapping, specify the fields to index using
                      mappings_fields. Mutually exclusive with mappings_dynamic_config.
                    type: boolean
                  mappingsDynamicConfig:
                    description: JSON object for mappings.dynamic when using configurable
                      dynamic. See the MongoDB documentation for further information
                      on Static and Dynamic Mapping. Mutually exclusive with mappings_dynamic.
                    type: string
                  mappingsFields:
                    description: attribute is required in search indexes when mappings_dynamic
                      is false. This field needs to be a JSON string in order to be
                      decoded correctly.
                    type: string
                  name:
                    description: The name of the search index you want to create.
                    type: string
                  numPartitions:
                    description: Number of index partitions. Allowed values are [1,
                      2, 4]. Default value is 1.
                    type: number
                  projectId:
                    description: The ID of the organization or project you want to
                      create the search index within.
                    type: string
                  searchAnalyzer:
                    description: Analyzer to use when searching the index. Defaults
                      to lucene.standard
                    type: string
                  status:
                    description: Current status of the index.
                    type: string
                  storedSource:
                    description: String that can be "true" (store all fields), "false"
                      (default, don't store any field), or a JSON string that contains
                      the list of fields to store (include) or not store (exclude)
                      on Atlas Search. To learn more, see Stored Source Fields.
                    type: string
                  synonyms:
                    description: Synonyms mapping definition to use in this index.
                    items:
                      properties:
                        analyzer:
                          description: Analyzer to use when creating the index. Defaults
                            to lucene.standard
                          type: string
                        name:
                          description: Name of the type set.
                          type: string
                        sourceCollection:
                          description: Name of the source MongoDB collection for the
                            synonyms. Documents in this collection must be in the
                            format described in the Synonyms Source Collection Documents.
                          type: string
                      type: object
                    type: array
                  type:
                    description: 'Type of index: search or vectorSearch. Default type
                      is search.'
                    type: string
                  typeSets:
                    description: One or more blocks defining configurable dynamic
                      type sets. Atlas only persists/returns typeSets when mappings.dynamic
                      is an object referencing a typeSet name.
                    items:
                      properties:
                        name:
                          description: Name of the type set.
                          type: string
                        types:
                          description: JSON array describing the types.
                          type: string
                      type: object
                    type: array
                  waitForIndexBuildCompletion:
                    type: boolean
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
