---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: connections.stream.mongodbatlas.crossplane.io
spec:
  group: stream.mongodbatlas.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - mongodbatlas
    kind: Connection
    listKind: ConnectionList
    plural: connections
    singular: connection
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Connection is the Schema for the Connections API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConnectionSpec defines the desired state of Connection
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  authentication:
                    description: User credentials required to connect to a Kafka cluster.
                      Includes the authentication type, as well as the parameters
                      for that authentication mode. See authentication.
                    properties:
                      clientId:
                        description: Public identifier for the Kafka client.
                        type: string
                      clientSecretSecretRef:
                        description: Secret known only to the Kafka client and the
                          authorization server.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      mechanism:
                        description: Method of authentication. Value can be PLAIN,
                          SCRAM-256, or SCRAM-512.
                        type: string
                      method:
                        description: SASL OAUTHBEARER authentication method. Value
                          must be OIDC.
                        type: string
                      passwordSecretRef:
                        description: Password of the account to connect to the Kafka
                          cluster.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      saslOauthbearerExtensions:
                        description: Additional information to provide to the Kafka
                          broker.
                        type: string
                      scope:
                        description: Scope of the access request to the broker specified
                          by the Kafka clients.
                        type: string
                      tokenEndpointUrl:
                        description: OAUTH issuer (IdP provider) token endpoint HTTP(S)
                          URI used to retrieve the token.
                        type: string
                      username:
                        description: Username of the account to connect to the Kafka
                          cluster.
                        type: string
                    type: object
                  aws:
                    description: The configuration for AWS Lambda connection. See
                      AWS
                    properties:
                      roleArn:
                        description: Amazon Resource Name (ARN) that identifies the
                          Amazon Web Services (AWS) Identity and Access Management
                          (IAM) role that MongoDB Cloud assumes when it accesses resources
                          in your AWS account.
                        type: string
                    type: object
                  bootstrapServers:
                    description: Comma separated list of server addresses.
                    type: string
                  clusterName:
                    description: Name of the cluster configured for this connection.
                    type: string
                  clusterProjectId:
                    description: Unique 24-hexadecimal digit string that identifies
                      the project that contains the configured cluster. Required if
                      the ID does not match the project containing the streams instance.
                      You must first enable the organization setting.
                    type: string
                  config:
                    additionalProperties:
                      type: string
                    description: A map of Kafka key-value pairs for optional configuration.
                      This is a flat object, and keys can have '.' characters.
                    type: object
                    x-kubernetes-map-type: granular
                  connectionName:
                    description: Label that identifies the stream connection. In the
                      case of the Sample type, this is the name of the sample source.
                    type: string
                  dbRoleToExecute:
                    description: The name of a Built in or Custom DB Role to connect
                      to an Atlas Cluster. See DBRoleToExecute.
                    properties:
                      role:
                        description: The name of the role to use. Value can be  atlasAdmin,
                          readWriteAnyDatabase, or readAnyDatabase if type is set
                          to BUILT_IN, or the name of a user-defined role if type
                          is set to CUSTOM.
                        type: string
                      type:
                        description: Type of connection. Can be AWSLambda, Cluster,
                          Https, Kafka, Sample, or SchemaRegistry.
                        type: string
                    type: object
                  headers:
                    additionalProperties:
                      type: string
                    description: A map of key-value pairs for optional headers.
                    type: object
                    x-kubernetes-map-type: granular
                  instanceName:
                    description: Label that identifies the stream processing workspace.
                      Use workspace_name instead; this attribute will be removed in
                      a future major version.
                    type: string
                  networking:
                    description: Networking Access Type can either be PUBLIC (default)
                      or VPC. See networking.
                    properties:
                      access:
                        description: Information about the networking access. See
                          access.
                        properties:
                          connectionId:
                            description: Id of the Private Link connection when type
                              is PRIVATE_LINK.
                            type: string
                          type:
                            description: Type of connection. Can be AWSLambda, Cluster,
                              Https, Kafka, Sample, or SchemaRegistry.
                            type: string
                        type: object
                    type: object
                  projectId:
                    description: Unique 24-hexadecimal digit string that identifies
                      your project.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  schemaRegistryAuthentication:
                    description: Authentication configuration for Schema Registry.
                      See Schema Registry Authentication.
                    properties:
                      passwordSecretRef:
                        description: Password of the account to connect to the Kafka
                          cluster.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      type:
                        description: Type of connection. Can be AWSLambda, Cluster,
                          Https, Kafka, Sample, or SchemaRegistry.
                        type: string
                      username:
                        description: Username of the account to connect to the Kafka
                          cluster.
                        type: string
                    type: object
                  schemaRegistryProvider:
                    description: The Schema Registry provider. Must be set to CONFLUENT.
                    type: string
                  schemaRegistryUrls:
                    description: List of Schema Registry endpoint URLs used by this
                      connection. Each URL must use the http or https scheme and specify
                      a valid host and optional port.
                    items:
                      type: string
                    type: array
                  security:
                    description: Properties for the secure transport connection to
                      Kafka. For SASL_SSL, this can include the trusted certificate
                      to use. See security.
                    properties:
                      brokerPublicCertificate:
                        description: A trusted, public x509 certificate for connecting
                          to Kafka over SSL. String value of the certificate must
                          be defined in the attribute.
                        type: string
                      protocol:
                        description: Describes the transport type. Can be either SASL_PLAINTEXT
                          or SASL_SSL.
                        type: string
                    type: object
                  timeouts:
                    properties:
                      create:
                        description: |-
                          The maximum time to wait for the stream connection to be fully provisioned after creation. Defaults to 20m (20 minutes).
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), and "h" (hours). Default: `20m`.
                        type: string
                      delete:
                        description: |-
                          The maximum time to wait for the stream connection to be fully deleted. Defaults to 10m (10 minutes).
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), and "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs. Default: `10m`.
                        type: string
                      update:
                        description: |-
                          The maximum time to wait for the stream connection to be fully provisioned after an update. Defaults to 20m (20 minutes).
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), and "h" (hours). Default: `20m`.
                        type: string
                    type: object
                  type:
                    description: Type of connection. Can be AWSLambda, Cluster, Https,
                      Kafka, Sample, or SchemaRegistry.
                    type: string
                  url:
                    description: URL of the HTTPs endpoint that will be used for creating
                      a connection.
                    type: string
                  workspaceName:
                    description: Label that identifies the stream processing workspace.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  authentication:
                    description: User credentials required to connect to a Kafka cluster.
                      Includes the authentication type, as well as the parameters
                      for that authentication mode. See authentication.
                    properties:
                      clientId:
                        description: Public identifier for the Kafka client.
                        type: string
                      clientSecretSecretRef:
                        description: Secret known only to the Kafka client and the
                          authorization server.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      mechanism:
                        description: Method of authentication. Value can be PLAIN,
                          SCRAM-256, or SCRAM-512.
                        type: string
                      method:
                        description: SASL OAUTHBEARER authentication method. Value
                          must be OIDC.
                        type: string
                      passwordSecretRef:
                        description: Password of the account to connect to the Kafka
                          cluster.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      saslOauthbearerExtensions:
                        description: Additional information to provide to the Kafka
                          broker.
                        type: string
                      scope:
                        description: Scope of the access request to the broker specified
                          by the Kafka clients.
                        type: string
                      tokenEndpointUrl:
                        description: OAUTH issuer (IdP provider) token endpoint HTTP(S)
                          URI used to retrieve the token.
                        type: string
                      username:
                        description: Username of the account to connect to the Kafka
                          cluster.
                        type: string
                    type: object
                  aws:
                    description: The configuration for AWS Lambda connection. See
                      AWS
                    properties:
                      roleArn:
                        description: Amazon Resource Name (ARN) that identifies the
                          Amazon Web Services (AWS) Identity and Access Management
                          (IAM) role that MongoDB Cloud assumes when it accesses resources
                          in your AWS account.
                        type: string
                    type: object
                  bootstrapServers:
                    description: Comma separated list of server addresses.
                    type: string
                  clusterName:
                    description: Name of the cluster configured for this connection.
                    type: string
                  clusterProjectId:
                    description: Unique 24-hexadecimal digit string that identifies
                      the project that contains the configured cluster. Required if
                      the ID does not match the project containing the streams instance.
                      You must first enable the organization setting.
                    type: string
                  config:
                    additionalProperties:
                      type: string
                    description: A map of Kafka key-value pairs for optional configuration.
                      This is a flat object, and keys can have '.' characters.
                    type: object
                    x-kubernetes-map-type: granular
                  connectionName:
                    description: Label that identifies the stream connection. In the
                      case of the Sample type, this is the name of the sample source.
                    type: string
                  dbRoleToExecute:
                    description: The name of a Built in or Custom DB Role to connect
                      to an Atlas Cluster. See DBRoleToExecute.
                    properties:
                      role:
                        description: The name of the role to use. Value can be  atlasAdmin,
                          readWriteAnyDatabase, or readAnyDatabase if type is set
                          to BUILT_IN, or the name of a user-defined role if type
                          is set to CUSTOM.
                        type: string
                      type:
                        description: Type of connection. Can be AWSLambda, Cluster,
                          Https, Kafka, Sample, or SchemaRegistry.
                        type: string
                    type: object
                  headers:
                    additionalProperties:
                      type: string
                    description: A map of key-value pairs for optional headers.
                    type: object
                    x-kubernetes-map-type: granular
                  instanceName:
                    description: Label that identifies the stream processing workspace.
                      Use workspace_name instead; this attribute will be removed in
                      a future major version.
                    type: string
                  networking:
                    description: Networking Access Type can either be PUBLIC (default)
                      or VPC. See networking.
                    properties:
                      access:
                        description: Information about the networking access. See
                          access.
                        properties:
                          connectionId:
                            description: Id of the Private Link connection when type
                              is PRIVATE_LINK.
                            type: string
                          type:
                            description: Type of connection. Can be AWSLambda, Cluster,
                              Https, Kafka, Sample, or SchemaRegistry.
                            type: string
                        type: object
                    type: object
                  projectId:
                    description: Unique 24-hexadecimal digit string that identifies
                      your project.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  schemaRegistryAuthentication:
                    description: Authentication configuration for Schema Registry.
                      See Schema Registry Authentication.
                    properties:
                      passwordSecretRef:
                        description: Password of the account to connect to the Kafka
                          cluster.
                        properties:
                          key:
                            description: The key to select.
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                          namespace:
                            description: Namespace of the secret.
                            type: string
                        required:
                        - key
                        - name
                        - namespace
                        type: object
                      type:
                        description: Type of connection. Can be AWSLambda, Cluster,
                          Https, Kafka, Sample, or SchemaRegistry.
                        type: string
                      username:
                        description: Username of the account to connect to the Kafka
                          cluster.
                        type: string
                    type: object
                  schemaRegistryProvider:
                    description: The Schema Registry provider. Must be set to CONFLUENT.
                    type: string
                  schemaRegistryUrls:
                    description: List of Schema Registry endpoint URLs used by this
                      connection. Each URL must use the http or https scheme and specify
                      a valid host and optional port.
                    items:
                      type: string
                    type: array
                  security:
                    description: Properties for the secure transport connection to
                      Kafka. For SASL_SSL, this can include the trusted certificate
                      to use. See security.
                    properties:
                      brokerPublicCertificate:
                        description: A trusted, public x509 certificate for connecting
                          to Kafka over SSL. String value of the certificate must
                          be defined in the attribute.
                        type: string
                      protocol:
                        description: Describes the transport type. Can be either SASL_PLAINTEXT
                          or SASL_SSL.
                        type: string
                    type: object
                  timeouts:
                    properties:
                      create:
                        description: |-
                          The maximum time to wait for the stream connection to be fully provisioned after creation. Defaults to 20m (20 minutes).
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), and "h" (hours). Default: `20m`.
                        type: string
                      delete:
                        description: |-
                          The maximum time to wait for the stream connection to be fully deleted. Defaults to 10m (10 minutes).
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), and "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs. Default: `10m`.
                        type: string
                      update:
                        description: |-
                          The maximum time to wait for the stream connection to be fully provisioned after an update. Defaults to 20m (20 minutes).
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), and "h" (hours). Default: `20m`.
                        type: string
                    type: object
                  type:
                    description: Type of connection. Can be AWSLambda, Cluster, Https,
                      Kafka, Sample, or SchemaRegistry.
                    type: string
                  url:
                    description: URL of the HTTPs endpoint that will be used for creating
                      a connection.
                    type: string
                  workspaceName:
                    description: Label that identifies the stream processing workspace.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.connectionName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.connectionName)
                || (has(self.initProvider) && has(self.initProvider.connectionName))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: ConnectionStatus defines the observed state of Connection.
            properties:
              atProvider:
                properties:
                  authentication:
                    description: User credentials required to connect to a Kafka cluster.
                      Includes the authentication type, as well as the parameters
                      for that authentication mode. See authentication.
                    properties:
                      clientId:
                        description: Public identifier for the Kafka client.
                        type: string
                      mechanism:
                        description: Method of authentication. Value can be PLAIN,
                          SCRAM-256, or SCRAM-512.
                        type: string
                      method:
                        description: SASL OAUTHBEARER authentication method. Value
                          must be OIDC.
                        type: string
                      saslOauthbearerExtensions:
                        description: Additional information to provide to the Kafka
                          broker.
                        type: string
                      scope:
                        description: Scope of the access request to the broker specified
                          by the Kafka clients.
                        type: string
                      tokenEndpointUrl:
                        description: OAUTH issuer (IdP provider) token endpoint HTTP(S)
                          URI used to retrieve the token.
                        type: string
                      username:
                        description: Username of the account to connect to the Kafka
                          cluster.
                        type: string
                    type: object
                  aws:
                    description: The configuration for AWS Lambda connection. See
                      AWS
                    properties:
                      roleArn:
                        description: Amazon Resource Name (ARN) that identifies the
                          Amazon Web Services (AWS) Identity and Access Management
                          (IAM) role that MongoDB Cloud assumes when it accesses resources
                          in your AWS account.
                        type: string
                    type: object
                  bootstrapServers:
                    description: Comma separated list of server addresses.
                    type: string
                  clusterName:
                    description: Name of the cluster configured for this connection.
                    type: string
                  clusterProjectId:
                    description: Unique 24-hexadecimal digit string that identifies
                      the project that contains the configured cluster. Required if
                      the ID does not match the project containing the streams instance.
                      You must first enable the organization setting.
                    type: string
                  config:
                    additionalProperties:
                      type: string
                    description: A map of Kafka key-value pairs for optional configuration.
                      This is a flat object, and keys can have '.' characters.
                    type: object
                    x-kubernetes-map-type: granular
                  connectionName:
                    description: Label that identifies the stream connection. In the
                      case of the Sample type, this is the name of the sample source.
                    type: string
                  dbRoleToExecute:
                    description: The name of a Built in or Custom DB Role to connect
                      to an Atlas Cluster. See DBRoleToExecute.
                    properties:
                      role:
                        description: The name of the role to use. Value can be  atlasAdmin,
                          readWriteAnyDatabase, or readAnyDatabase if type is set
                          to BUILT_IN, or the name of a user-defined role if type
                          is set to CUSTOM.
                        type: string
                      type:
                        description: Type of connection. Can be AWSLambda, Cluster,
                          Https, Kafka, Sample, or SchemaRegistry.
                        type: string
                    type: object
                  headers:
                    additionalProperties:
                      type: string
                    description: A map of key-value pairs for optional headers.
                    type: object
                    x-kubernetes-map-type: granular
                  id:
                    type: string
                  instanceName:
                    description: Label that identifies the stream processing workspace.
                      Use workspace_name instead; this attribute will be removed in
                      a future major version.
                    type: string
                  networking:
                    description: Networking Access Type can either be PUBLIC (default)
                      or VPC. See networking.
                    properties:
                      access:
                        description: Information about the networking access. See
                          access.
                        properties:
                          connectionId:
                            description: Id of the Private Link connection when type
                              is PRIVATE_LINK.
                            type: string
                          type:
                            description: Type of connection. Can be AWSLambda, Cluster,
                              Https, Kafka, Sample, or SchemaRegistry.
                            type: string
                        type: object
                    type: object
                  projectId:
                    description: Unique 24-hexadecimal digit string that identifies
                      your project.
                    type: string
                  schemaRegistryAuthentication:
                    description: Authentication configuration for Schema Registry.
                      See Schema Registry Authentication.
                    properties:
                      type:
                        description: Type of connection. Can be AWSLambda, Cluster,
                          Https, Kafka, Sample, or SchemaRegistry.
                        type: string
                      username:
                        description: Username of the account to connect to the Kafka
                          cluster.
                        type: string
                    type: object
                  schemaRegistryProvider:
                    description: The Schema Registry provider. Must be set to CONFLUENT.
                    type: string
                  schemaRegistryUrls:
                    description: List of Schema Registry endpoint URLs used by this
                      connection. Each URL must use the http or https scheme and specify
                      a valid host and optional port.
                    items:
                      type: string
                    type: array
                  security:
                    description: Properties for the secure transport connection to
                      Kafka. For SASL_SSL, this can include the trusted certificate
                      to use. See security.
                    properties:
                      brokerPublicCertificate:
                        description: A trusted, public x509 certificate for connecting
                          to Kafka over SSL. String value of the certificate must
                          be defined in the attribute.
                        type: string
                      protocol:
                        description: Describes the transport type. Can be either SASL_PLAINTEXT
                          or SASL_SSL.
                        type: string
                    type: object
                  timeouts:
                    properties:
                      create:
                        description: |-
                          The maximum time to wait for the stream connection to be fully provisioned after creation. Defaults to 20m (20 minutes).
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), and "h" (hours). Default: `20m`.
                        type: string
                      delete:
                        description: |-
                          The maximum time to wait for the stream connection to be fully deleted. Defaults to 10m (10 minutes).
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), and "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs. Default: `10m`.
                        type: string
                      update:
                        description: |-
                          The maximum time to wait for the stream connection to be fully provisioned after an update. Defaults to 20m (20 minutes).
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), and "h" (hours). Default: `20m`.
                        type: string
                    type: object
                  type:
                    description: Type of connection. Can be AWSLambda, Cluster, Https,
                      Kafka, Sample, or SchemaRegistry.
                    type: string
                  url:
                    description: URL of the HTTPs endpoint that will be used for creating
                      a connection.
                    type: string
                  workspaceName:
                    description: Label that identifies the stream processing workspace.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
