---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: connectionfailovers.stream.mongodbatlas.m.crossplane.io
spec:
  group: stream.mongodbatlas.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - mongodbatlas
    kind: ConnectionFailover
    listKind: ConnectionFailoverList
    plural: connectionfailovers
    singular: connectionfailover
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ConnectionFailover is the Schema for the ConnectionFailovers
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ConnectionFailoverSpec defines the desired state of ConnectionFailover
            properties:
              forProvider:
                properties:
                  authentication:
                    description: (Attributes) User credentials required to connect
                      to a Kafka Cluster. Includes the authentication type, as well
                      as the parameters for that authentication mode. (see below for
                      nested schema)
                    properties:
                      clientId:
                        description: |-
                          (String) OIDC client identifier for authentication to the Kafka cluster.
                          OIDC client identifier for authentication to the Kafka cluster.
                        type: string
                      clientSecretSecretRef:
                        description: |-
                          (String, Sensitive) OIDC client secret for authentication to the Kafka cluster.
                          OIDC client secret for authentication to the Kafka cluster.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      mechanism:
                        description: |-
                          256, SCRAM-512, or OAUTHBEARER.
                          Style of authentication. Can be one of PLAIN, SCRAM-256, SCRAM-512, or OAUTHBEARER.
                        type: string
                      method:
                        description: |-
                          (String) SASL OAUTHBEARER authentication method. Currently, only OIDC is supported.
                          SASL OAUTHBEARER authentication method. Currently, only OIDC is supported.
                        type: string
                      passwordSecretRef:
                        description: |-
                          (String, Sensitive) Password of the account to connect to the Kafka cluster.
                          Password of the account to connect to the Kafka cluster.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      saslOauthbearerExtensions:
                        description: |-
                          (String) SASL OAUTHBEARER extensions parameter for additional OAuth2 configuration.
                          SASL OAUTHBEARER extensions parameter for additional OAuth2 configuration.
                        type: string
                      scope:
                        description: |-
                          (String) OIDC scope parameter defining the access permissions requested.
                          OIDC scope parameter defining the access permissions requested.
                        type: string
                      sslCertificate:
                        description: |-
                          (String) SSL certificate for client authentication to Kafka.
                          SSL certificate for client authentication to Kafka.
                        type: string
                      sslKeyPasswordSecretRef:
                        description: |-
                          (String, Sensitive) Password for the SSL key, if it is password protected.
                          Password for the SSL key, if it is password protected.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      sslKeySecretRef:
                        description: |-
                          (String, Sensitive) SSL key for client authentication to Kafka.
                          SSL key for client authentication to Kafka.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      tokenEndpointUrl:
                        description: |-
                          (String) OIDC token endpoint URL for obtaining access tokens.
                          OIDC token endpoint URL for obtaining access tokens.
                        type: string
                      username:
                        description: |-
                          (String) Username of the account to connect to the Kafka cluster.
                          Username of the account to connect to the Kafka cluster.
                        type: string
                    type: object
                  bootstrapServers:
                    description: |-
                      (String) Comma separated list of server addresses.
                      Optional for type: Kafka. Comma separated list of server addresses.
                    type: string
                  clusterName:
                    description: |-
                      (String) Name of the cluster configured for this connection.
                      Optional for type: Cluster. Name of the cluster configured for this connection.
                    type: string
                  clusterProjectId:
                    description: |-
                      hexadecimal digit string that identifies the project that contains the configured cluster. Required if the ID does not match the project containing the streams workspace. You must first enable the organization setting.
                      Optional for type: Cluster. Unique 24-hexadecimal digit string that identifies the project that contains the configured cluster. Required if the ID does not match the project containing the streams workspace. You must first enable the organization setting.
                    type: string
                  config:
                    additionalProperties:
                      type: string
                    description: |-
                      value pairs for optional configuration. This object is flat, and keys can have '.' characters.
                      Optional for type: Kafka. Map of Kafka key-value pairs for optional configuration. This object is flat, and keys can have '.' characters.
                    type: object
                    x-kubernetes-map-type: granular
                  connectionName:
                    description: |-
                      (String) Label that identifies the stream connection name.
                      Label that identifies the stream connection name.
                    type: string
                  connectionNameRef:
                    description: Reference to a Connection in stream to populate connectionName.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  connectionNameSelector:
                    description: Selector for a Connection in stream to populate connectionName.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dbRoleToExecute:
                    description: in or custom DB Role to connect to a MongoDB Cloud
                      Cluster. (see below for nested schema)
                    properties:
                      role:
                        description: |-
                          (String) The name of the role to use. Can be a built in role or a custom role.
                          The name of the role to use. Can be a built in role or a custom role.
                        type: string
                      type:
                        description: |-
                          (String) Connection type.
                          Type of the DB role. Can be either Built In or Custom.
                        type: string
                    type: object
                  deleteOnCreateTimeout:
                    description: |-
                      (Boolean) Indicates whether to delete the resource being created if a timeout is reached when waiting for completion. When set to true and timeout occurs, it triggers the deletion and returns immediately without waiting for deletion to complete. When set to false, the timeout will not trigger resource deletion. If you suspect a transient error when the value is true, wait before retrying to allow resource deletion to finish. Default is true.
                      Indicates whether to delete the resource being created if a timeout is reached when waiting for completion. When set to `true` and timeout occurs, it triggers the deletion and returns immediately without waiting for deletion to complete. When set to `false`, the timeout will not trigger resource deletion. If you suspect a transient error when the value is `true`, wait before retrying to allow resource deletion to finish. Default is `true`.
                    type: boolean
                  networking:
                    description: (Attributes) Networking configuration for Streams
                      connections. (see below for nested schema)
                    properties:
                      access:
                        description: (Attributes) Information about networking access.
                          (see below for nested schema)
                        properties:
                          connectionId:
                            description: |-
                              (String) Reserved. Will be used by PRIVATE_LINK connection type.
                              Reserved. Will be used by `PRIVATE_LINK` connection type.
                            type: string
                          name:
                            description: |-
                              (String) Reserved. Will be used by PRIVATE_LINK connection type.
                              Reserved. Will be used by `PRIVATE_LINK` connection type.
                            type: string
                          tgwRouteId:
                            description: |-
                              (String) Reserved. Will be used by TRANSIT_GATEWAY connection type.
                              Reserved. Will be used by `TRANSIT_GATEWAY` connection type.
                            type: string
                          type:
                            description: |-
                              (String) Connection type.
                              Selected networking type. Either `PUBLIC`, `VPC`, `PRIVATE_LINK`, or `TRANSIT_GATEWAY`. Defaults to `PUBLIC`. For VPC, ensure that VPC peering exists and connectivity has been established between Atlas VPC and the VPC where Kafka cluster is hosted for the connection to function properly. `TRANSIT_GATEWAY` support is coming soon.
                            type: string
                        type: object
                    type: object
                  projectId:
                    description: |-
                      hexadecimal digit string that identifies your project, also known as groupId in the official documentation.
                      Unique 24-hexadecimal digit string that identifies your project, also known as `groupId` in the official documentation.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  region:
                    description: |-
                      (String) Connection region.
                      Connection region.
                    type: string
                  security:
                    description: (Attributes) Properties for the secure transport
                      connection to Kafka. For SSL, this can include the trusted certificate
                      to use. (see below for nested schema)
                    properties:
                      brokerPublicCertificate:
                        description: |-
                          (String) A trusted, public x509 certificate for connecting to Kafka over SSL.
                          A trusted, public x509 certificate for connecting to Kafka over SSL.
                        type: string
                      protocol:
                        description: |-
                          (String) Describes the transport type. Can be either SASL_PLAINTEXT, SASL_SSL, or SSL.
                          Describes the transport type. Can be either `SASL_PLAINTEXT`, `SASL_SSL`, or `SSL`.
                        type: string
                    type: object
                  timeouts:
                    description: (Attributes) (see below for nested schema)
                    properties:
                      create:
                        description: |-
                          (String) A string that can be parsed as a duration consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
                        type: string
                      delete:
                        description: |-
                          (String) A string that can be parsed as a duration consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
                        type: string
                      update:
                        description: |-
                          (String) A string that can be parsed as a duration consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
                        type: string
                    type: object
                  type:
                    description: |-
                      (String) Connection type.
                      Connection type.
                    type: string
                  workspaceName:
                    description: |-
                      (String) Label that identifies the stream workspace.
                      Label that identifies the stream workspace.
                    type: string
                  workspaceNameRef:
                    description: Reference to a Workspace in stream to populate workspaceName.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  workspaceNameSelector:
                    description: Selector for a Workspace in stream to populate workspaceName.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  authentication:
                    description: (Attributes) User credentials required to connect
                      to a Kafka Cluster. Includes the authentication type, as well
                      as the parameters for that authentication mode. (see below for
                      nested schema)
                    properties:
                      clientId:
                        description: |-
                          (String) OIDC client identifier for authentication to the Kafka cluster.
                          OIDC client identifier for authentication to the Kafka cluster.
                        type: string
                      clientSecretSecretRef:
                        description: |-
                          (String, Sensitive) OIDC client secret for authentication to the Kafka cluster.
                          OIDC client secret for authentication to the Kafka cluster.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      mechanism:
                        description: |-
                          256, SCRAM-512, or OAUTHBEARER.
                          Style of authentication. Can be one of PLAIN, SCRAM-256, SCRAM-512, or OAUTHBEARER.
                        type: string
                      method:
                        description: |-
                          (String) SASL OAUTHBEARER authentication method. Currently, only OIDC is supported.
                          SASL OAUTHBEARER authentication method. Currently, only OIDC is supported.
                        type: string
                      passwordSecretRef:
                        description: |-
                          (String, Sensitive) Password of the account to connect to the Kafka cluster.
                          Password of the account to connect to the Kafka cluster.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      saslOauthbearerExtensions:
                        description: |-
                          (String) SASL OAUTHBEARER extensions parameter for additional OAuth2 configuration.
                          SASL OAUTHBEARER extensions parameter for additional OAuth2 configuration.
                        type: string
                      scope:
                        description: |-
                          (String) OIDC scope parameter defining the access permissions requested.
                          OIDC scope parameter defining the access permissions requested.
                        type: string
                      sslCertificate:
                        description: |-
                          (String) SSL certificate for client authentication to Kafka.
                          SSL certificate for client authentication to Kafka.
                        type: string
                      sslKeyPasswordSecretRef:
                        description: |-
                          (String, Sensitive) Password for the SSL key, if it is password protected.
                          Password for the SSL key, if it is password protected.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      sslKeySecretRef:
                        description: |-
                          (String, Sensitive) SSL key for client authentication to Kafka.
                          SSL key for client authentication to Kafka.
                        properties:
                          key:
                            type: string
                          name:
                            description: Name of the secret.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      tokenEndpointUrl:
                        description: |-
                          (String) OIDC token endpoint URL for obtaining access tokens.
                          OIDC token endpoint URL for obtaining access tokens.
                        type: string
                      username:
                        description: |-
                          (String) Username of the account to connect to the Kafka cluster.
                          Username of the account to connect to the Kafka cluster.
                        type: string
                    type: object
                  bootstrapServers:
                    description: |-
                      (String) Comma separated list of server addresses.
                      Optional for type: Kafka. Comma separated list of server addresses.
                    type: string
                  clusterName:
                    description: |-
                      (String) Name of the cluster configured for this connection.
                      Optional for type: Cluster. Name of the cluster configured for this connection.
                    type: string
                  clusterProjectId:
                    description: |-
                      hexadecimal digit string that identifies the project that contains the configured cluster. Required if the ID does not match the project containing the streams workspace. You must first enable the organization setting.
                      Optional for type: Cluster. Unique 24-hexadecimal digit string that identifies the project that contains the configured cluster. Required if the ID does not match the project containing the streams workspace. You must first enable the organization setting.
                    type: string
                  config:
                    additionalProperties:
                      type: string
                    description: |-
                      value pairs for optional configuration. This object is flat, and keys can have '.' characters.
                      Optional for type: Kafka. Map of Kafka key-value pairs for optional configuration. This object is flat, and keys can have '.' characters.
                    type: object
                    x-kubernetes-map-type: granular
                  connectionName:
                    description: |-
                      (String) Label that identifies the stream connection name.
                      Label that identifies the stream connection name.
                    type: string
                  connectionNameRef:
                    description: Reference to a Connection in stream to populate connectionName.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  connectionNameSelector:
                    description: Selector for a Connection in stream to populate connectionName.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  dbRoleToExecute:
                    description: in or custom DB Role to connect to a MongoDB Cloud
                      Cluster. (see below for nested schema)
                    properties:
                      role:
                        description: |-
                          (String) The name of the role to use. Can be a built in role or a custom role.
                          The name of the role to use. Can be a built in role or a custom role.
                        type: string
                      type:
                        description: |-
                          (String) Connection type.
                          Type of the DB role. Can be either Built In or Custom.
                        type: string
                    type: object
                  deleteOnCreateTimeout:
                    description: |-
                      (Boolean) Indicates whether to delete the resource being created if a timeout is reached when waiting for completion. When set to true and timeout occurs, it triggers the deletion and returns immediately without waiting for deletion to complete. When set to false, the timeout will not trigger resource deletion. If you suspect a transient error when the value is true, wait before retrying to allow resource deletion to finish. Default is true.
                      Indicates whether to delete the resource being created if a timeout is reached when waiting for completion. When set to `true` and timeout occurs, it triggers the deletion and returns immediately without waiting for deletion to complete. When set to `false`, the timeout will not trigger resource deletion. If you suspect a transient error when the value is `true`, wait before retrying to allow resource deletion to finish. Default is `true`.
                    type: boolean
                  networking:
                    description: (Attributes) Networking configuration for Streams
                      connections. (see below for nested schema)
                    properties:
                      access:
                        description: (Attributes) Information about networking access.
                          (see below for nested schema)
                        properties:
                          connectionId:
                            description: |-
                              (String) Reserved. Will be used by PRIVATE_LINK connection type.
                              Reserved. Will be used by `PRIVATE_LINK` connection type.
                            type: string
                          name:
                            description: |-
                              (String) Reserved. Will be used by PRIVATE_LINK connection type.
                              Reserved. Will be used by `PRIVATE_LINK` connection type.
                            type: string
                          tgwRouteId:
                            description: |-
                              (String) Reserved. Will be used by TRANSIT_GATEWAY connection type.
                              Reserved. Will be used by `TRANSIT_GATEWAY` connection type.
                            type: string
                          type:
                            description: |-
                              (String) Connection type.
                              Selected networking type. Either `PUBLIC`, `VPC`, `PRIVATE_LINK`, or `TRANSIT_GATEWAY`. Defaults to `PUBLIC`. For VPC, ensure that VPC peering exists and connectivity has been established between Atlas VPC and the VPC where Kafka cluster is hosted for the connection to function properly. `TRANSIT_GATEWAY` support is coming soon.
                            type: string
                        type: object
                    type: object
                  projectId:
                    description: |-
                      hexadecimal digit string that identifies your project, also known as groupId in the official documentation.
                      Unique 24-hexadecimal digit string that identifies your project, also known as `groupId` in the official documentation.
                    type: string
                  projectIdRef:
                    description: Reference to a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  projectIdSelector:
                    description: Selector for a Project in mongodbatlas to populate
                      projectId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  region:
                    description: |-
                      (String) Connection region.
                      Connection region.
                    type: string
                  security:
                    description: (Attributes) Properties for the secure transport
                      connection to Kafka. For SSL, this can include the trusted certificate
                      to use. (see below for nested schema)
                    properties:
                      brokerPublicCertificate:
                        description: |-
                          (String) A trusted, public x509 certificate for connecting to Kafka over SSL.
                          A trusted, public x509 certificate for connecting to Kafka over SSL.
                        type: string
                      protocol:
                        description: |-
                          (String) Describes the transport type. Can be either SASL_PLAINTEXT, SASL_SSL, or SSL.
                          Describes the transport type. Can be either `SASL_PLAINTEXT`, `SASL_SSL`, or `SSL`.
                        type: string
                    type: object
                  timeouts:
                    description: (Attributes) (see below for nested schema)
                    properties:
                      create:
                        description: |-
                          (String) A string that can be parsed as a duration consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
                        type: string
                      delete:
                        description: |-
                          (String) A string that can be parsed as a duration consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
                        type: string
                      update:
                        description: |-
                          (String) A string that can be parsed as a duration consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
                        type: string
                    type: object
                  type:
                    description: |-
                      (String) Connection type.
                      Connection type.
                    type: string
                  workspaceName:
                    description: |-
                      (String) Label that identifies the stream workspace.
                      Label that identifies the stream workspace.
                    type: string
                  workspaceNameRef:
                    description: Reference to a Workspace in stream to populate workspaceName.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  workspaceNameSelector:
                    description: Selector for a Workspace in stream to populate workspaceName.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.region is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.region)
                || (has(self.initProvider) && has(self.initProvider.region))'
            - message: spec.forProvider.type is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.type)
                || (has(self.initProvider) && has(self.initProvider.type))'
          status:
            description: ConnectionFailoverStatus defines the observed state of ConnectionFailover.
            properties:
              atProvider:
                properties:
                  authentication:
                    description: (Attributes) User credentials required to connect
                      to a Kafka Cluster. Includes the authentication type, as well
                      as the parameters for that authentication mode. (see below for
                      nested schema)
                    properties:
                      clientId:
                        description: |-
                          (String) OIDC client identifier for authentication to the Kafka cluster.
                          OIDC client identifier for authentication to the Kafka cluster.
                        type: string
                      mechanism:
                        description: |-
                          256, SCRAM-512, or OAUTHBEARER.
                          Style of authentication. Can be one of PLAIN, SCRAM-256, SCRAM-512, or OAUTHBEARER.
                        type: string
                      method:
                        description: |-
                          (String) SASL OAUTHBEARER authentication method. Currently, only OIDC is supported.
                          SASL OAUTHBEARER authentication method. Currently, only OIDC is supported.
                        type: string
                      saslOauthbearerExtensions:
                        description: |-
                          (String) SASL OAUTHBEARER extensions parameter for additional OAuth2 configuration.
                          SASL OAUTHBEARER extensions parameter for additional OAuth2 configuration.
                        type: string
                      scope:
                        description: |-
                          (String) OIDC scope parameter defining the access permissions requested.
                          OIDC scope parameter defining the access permissions requested.
                        type: string
                      sslCertificate:
                        description: |-
                          (String) SSL certificate for client authentication to Kafka.
                          SSL certificate for client authentication to Kafka.
                        type: string
                      tokenEndpointUrl:
                        description: |-
                          (String) OIDC token endpoint URL for obtaining access tokens.
                          OIDC token endpoint URL for obtaining access tokens.
                        type: string
                      username:
                        description: |-
                          (String) Username of the account to connect to the Kafka cluster.
                          Username of the account to connect to the Kafka cluster.
                        type: string
                    type: object
                  bootstrapServers:
                    description: |-
                      (String) Comma separated list of server addresses.
                      Optional for type: Kafka. Comma separated list of server addresses.
                    type: string
                  clusterName:
                    description: |-
                      (String) Name of the cluster configured for this connection.
                      Optional for type: Cluster. Name of the cluster configured for this connection.
                    type: string
                  clusterProjectId:
                    description: |-
                      hexadecimal digit string that identifies the project that contains the configured cluster. Required if the ID does not match the project containing the streams workspace. You must first enable the organization setting.
                      Optional for type: Cluster. Unique 24-hexadecimal digit string that identifies the project that contains the configured cluster. Required if the ID does not match the project containing the streams workspace. You must first enable the organization setting.
                    type: string
                  config:
                    additionalProperties:
                      type: string
                    description: |-
                      value pairs for optional configuration. This object is flat, and keys can have '.' characters.
                      Optional for type: Kafka. Map of Kafka key-value pairs for optional configuration. This object is flat, and keys can have '.' characters.
                    type: object
                    x-kubernetes-map-type: granular
                  connectionName:
                    description: |-
                      (String) Label that identifies the stream connection name.
                      Label that identifies the stream connection name.
                    type: string
                  dbRoleToExecute:
                    description: in or custom DB Role to connect to a MongoDB Cloud
                      Cluster. (see below for nested schema)
                    properties:
                      role:
                        description: |-
                          (String) The name of the role to use. Can be a built in role or a custom role.
                          The name of the role to use. Can be a built in role or a custom role.
                        type: string
                      type:
                        description: |-
                          (String) Connection type.
                          Type of the DB role. Can be either Built In or Custom.
                        type: string
                    type: object
                  deleteOnCreateTimeout:
                    description: |-
                      (Boolean) Indicates whether to delete the resource being created if a timeout is reached when waiting for completion. When set to true and timeout occurs, it triggers the deletion and returns immediately without waiting for deletion to complete. When set to false, the timeout will not trigger resource deletion. If you suspect a transient error when the value is true, wait before retrying to allow resource deletion to finish. Default is true.
                      Indicates whether to delete the resource being created if a timeout is reached when waiting for completion. When set to `true` and timeout occurs, it triggers the deletion and returns immediately without waiting for deletion to complete. When set to `false`, the timeout will not trigger resource deletion. If you suspect a transient error when the value is `true`, wait before retrying to allow resource deletion to finish. Default is `true`.
                    type: boolean
                  failoverConnectionId:
                    description: |-
                      (String) Unique identifier of the connection.
                      Unique identifier of the connection.
                    type: string
                  id:
                    type: string
                  networking:
                    description: (Attributes) Networking configuration for Streams
                      connections. (see below for nested schema)
                    properties:
                      access:
                        description: (Attributes) Information about networking access.
                          (see below for nested schema)
                        properties:
                          connectionId:
                            description: |-
                              (String) Reserved. Will be used by PRIVATE_LINK connection type.
                              Reserved. Will be used by `PRIVATE_LINK` connection type.
                            type: string
                          name:
                            description: |-
                              (String) Reserved. Will be used by PRIVATE_LINK connection type.
                              Reserved. Will be used by `PRIVATE_LINK` connection type.
                            type: string
                          tgwRouteId:
                            description: |-
                              (String) Reserved. Will be used by TRANSIT_GATEWAY connection type.
                              Reserved. Will be used by `TRANSIT_GATEWAY` connection type.
                            type: string
                          type:
                            description: |-
                              (String) Connection type.
                              Selected networking type. Either `PUBLIC`, `VPC`, `PRIVATE_LINK`, or `TRANSIT_GATEWAY`. Defaults to `PUBLIC`. For VPC, ensure that VPC peering exists and connectivity has been established between Atlas VPC and the VPC where Kafka cluster is hosted for the connection to function properly. `TRANSIT_GATEWAY` support is coming soon.
                            type: string
                        type: object
                    type: object
                  projectId:
                    description: |-
                      hexadecimal digit string that identifies your project, also known as groupId in the official documentation.
                      Unique 24-hexadecimal digit string that identifies your project, also known as `groupId` in the official documentation.
                    type: string
                  region:
                    description: |-
                      (String) Connection region.
                      Connection region.
                    type: string
                  security:
                    description: (Attributes) Properties for the secure transport
                      connection to Kafka. For SSL, this can include the trusted certificate
                      to use. (see below for nested schema)
                    properties:
                      brokerPublicCertificate:
                        description: |-
                          (String) A trusted, public x509 certificate for connecting to Kafka over SSL.
                          A trusted, public x509 certificate for connecting to Kafka over SSL.
                        type: string
                      protocol:
                        description: |-
                          (String) Describes the transport type. Can be either SASL_PLAINTEXT, SASL_SSL, or SSL.
                          Describes the transport type. Can be either `SASL_PLAINTEXT`, `SASL_SSL`, or `SSL`.
                        type: string
                    type: object
                  state:
                    description: |-
                      (String) Connection state.
                      Connection state.
                    type: string
                  timeouts:
                    description: (Attributes) (see below for nested schema)
                    properties:
                      create:
                        description: |-
                          (String) A string that can be parsed as a duration consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
                        type: string
                      delete:
                        description: |-
                          (String) A string that can be parsed as a duration consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours). Setting a timeout for a Delete operation is only applicable if changes are saved into state before the destroy operation occurs.
                        type: string
                      update:
                        description: |-
                          (String) A string that can be parsed as a duration consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
                          A string that can be [parsed as a duration](https://pkg.go.dev/time#ParseDuration) consisting of numbers and unit suffixes, such as "30s" or "2h45m". Valid time units are "s" (seconds), "m" (minutes), "h" (hours).
                        type: string
                    type: object
                  type:
                    description: |-
                      (String) Connection type.
                      Connection type.
                    type: string
                  workspaceName:
                    description: |-
                      (String) Label that identifies the stream workspace.
                      Label that identifies the stream workspace.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
